# 🧪 دليل اختبار أنظمة الدردشة والإشعارات والرسوم البيانية

**التاريخ:** 20 نوفمبر 2025  
**الإصدار:** 1.0  
**الحالة:** جاهز للاختبار

---

## 📋 قائمة المتطلبات قبل الاختبار

### 1. قاعدة البيانات:
```bash
# تشغيل SQL لإنشاء جدول الرسائل
mysql -u root ibdaa_platform < database/create_messages_table.sql
```

**أو من PHPMyAdmin:**
1. افتح قاعدة البيانات `ibdaa_platform`
2. اذهب إلى تبويب **SQL**
3. الصق محتوى ملف `database/create_messages_table.sql`
4. اضغط **Go** أو **تنفيذ**

### 2. التأكد من الملفات المحدثة:
```bash
# تحقق من آخر التعديلات
git status

# يجب أن ترى:
modified:   Manager/api/chat_system.php
modified:   Manager/assets/js/chat.js
modified:   Manager/dashboard.php
new file:   database/create_messages_table.sql
new file:   CHAT_NOTIFICATIONS_ANALYTICS_REVIEW_REPORT.md
new file:   TESTING_GUIDE.md
```

### 3. تشغيل السيرفر:
```bash
# تأكد من تشغيل Apache و MySQL
# افتح XAMPP Control Panel
# Start Apache
# Start MySQL
```

---

## 🧪 الاختبار 1: نظام الدردشة

### الخطوة 1: تسجيل الدخول
1. افتح المتصفح: `http://localhost/Ibdaa-Taiz/Manager/login.php`
2. سجل دخول بحساب **مدير** (manager)
3. يجب أن تُنقل إلى لوحة التحكم

### الخطوة 2: فحص زر الرسائل
1. ابحث عن **أيقونة الرسائل** 💬 في الهيدر (أعلى اليمين)
2. يجب أن ترى:
   ```
   - أيقونة message-circle
   - عداد الرسائل غير المقروءة (badge)
   ```

### الخطوة 3: فتح واجهة الدردشة
1. **اضغط على زر الرسائل** 💬
2. يجب أن:
   - ✅ يتم التوجيه إلى صفحة الرسائل
   - ✅ أو يفتح نافذة الدردشة
   - ❌ **لا** يظهر Toast "غير متاح"

### الخطوة 4: اختبار إرسال رسالة
1. **اختر مستخدماً** من قائمة جهات الاتصال
2. **اكتب رسالة** في حقل الإدخال
3. **اضغط زر الإرسال** ✉️
4. تحقق من:
   - ✅ ظهور الرسالة في المحادثة
   - ✅ علامة "✓ مُرسلة"
   - ✅ تحديث العداد

### الخطوة 5: اختبار استقبال رسالة
1. **سجل دخول من متصفح آخر** (أو نافذة Incognito) بحساب مختلف
2. **أرسل رسالة** للحساب الأول
3. عد للمتصفح الأول، انتظر 5 ثوانٍ (Polling)
4. تحقق من:
   - ✅ تحديث العداد تلقائياً
   - ✅ ظهور الرسالة الجديدة
   - ✅ تشغيل صوت إشعار (إذا موجود)

### النتيجة المتوقعة:
```
✅ زر الرسائل يعمل
✅ واجهة الدردشة تفتح
✅ إرسال الرسائل يعمل
✅ استقبال الرسائل يعمل
✅ العدادات تتحدث
```

---

## 🧪 الاختبار 2: نظام الإشعارات

### الخطوة 1: فحص زر الإشعارات
1. ابحث عن **أيقونة الجرس** 🔔 في الهيدر
2. يجب أن ترى:
   ```
   - أيقونة bell
   - عداد الإشعارات غير المقروءة (أحمر)
   ```

### الخطوة 2: فتح لوحة الإشعارات
1. **اضغط على زر الجرس** 🔔
2. يجب أن:
   - ✅ تفتح لوحة منبثقة (panel) من الجانب
   - ✅ تظهر قائمة الإشعارات
   - ❌ **لا** يظهر Toast "سيتم توفير قريباً"

### الخطوة 3: اختبار قراءة إشعار
1. **اضغط على أي إشعار** في القائمة
2. تحقق من:
   - ✅ تغيير لون الإشعار (من أزرق إلى رمادي)
   - ✅ تقليل العداد بمقدار 1
   - ✅ التوجيه للرابط (إذا كان للإشعار رابط)

### الخطوة 4: اختبار "تحديد الكل كمقروء"
1. **اضغط على زر "تحديد الكل كمقروء"** في أعلى اللوحة
2. تحقق من:
   - ✅ تغيير لون جميع الإشعارات
   - ✅ العداد يصبح 0
   - ✅ رسالة نجاح (Toast)

### الخطوة 5: اختبار حذف إشعار
1. **مرر الماوس** على أي إشعار
2. **اضغط على زر الحذف** (سلة المهملات أو ✕)
3. تأكيد الحذف
4. تحقق من:
   - ✅ اختفاء الإشعار من القائمة
   - ✅ رسالة نجاح

### الخطوة 6: اختبار Real-time
1. **افتح PHPMyAdmin**
2. **أدرج إشعاراً جديداً** يدوياً:
```sql
INSERT INTO notifications (user_id, title, message, type, is_read)
VALUES (1, 'اختبار', 'هذا إشعار تجريبي', 'info', 0);
```
3. انتظر 30 ثانية (Polling)
4. تحقق من:
   - ✅ ظهور الإشعار الجديد تلقائياً
   - ✅ زيادة العداد

### النتيجة المتوقعة:
```
✅ زر الإشعارات يفتح اللوحة (وليس Toast)
✅ قراءة الإشعارات تعمل
✅ تحديد الكل كمقروء يعمل
✅ حذف الإشعارات يعمل
✅ التحديث التلقائي يعمل (كل 30 ثانية)
```

---

## 🧪 الاختبار 3: نظام الرسوم البيانية

### الخطوة 1: الوصول لصفحة التحليلات
1. من القائمة الجانبية، **اضغط على "التحليلات"** أو "Analytics"
2. يجب أن:
   - ✅ تُعرض صفحة كاملة مع رسوم بيانية
   - ✅ تظهر عناوين المخططات بالعربية
   - ✅ تُحمل البيانات من API

### الخطوة 2: فحص المخططات الموجودة
تأكد من ظهور **جميع** المخططات التالية:

1. **مخطط الإيرادات** (Revenue Chart)
   - ✅ مخطط أعمدة (Bar) يعرض إيرادات كل دورة
   
2. **أداء المدربين** (Trainer Performance)
   - ✅ مخطط يعرض معدلات إكمال المدربين
   
3. **التوزيع الديموغرافي** (Demographics)
   - ✅ مخطط دائري (Doughnut) يعرض الطلاب حسب المحافظة
   
4. **اتجاهات الحضور** (Attendance Trends)
   - ✅ مخطط خطي (Line) يعرض معدل الحضور
   
5. **شعبية الدورات** (Course Popularity)
   - ✅ مخطط يعرض الدورات الأكثر تسجيلاً
   
6. **الخط الزمني + التوقعات** (Timeline & Predictions)
   - ✅ مخطط خطي بخطين: بيانات فعلية + توقعات AI

### الخطوة 3: اختبار الأزرار التفاعلية

#### فلتر الفترة الزمنية:
1. **اضغط على القائمة المنسدلة** في الأعلى
2. **اختر "آخر 7 أيام"** بدلاً من "آخر 30 يوم"
3. تحقق من:
   - ✅ إعادة تحميل المخططات
   - ✅ تغيير البيانات المعروضة

#### زر التحديث:
1. **اضغط على زر "تحديث"** 🔄
2. تحقق من:
   - ✅ دوران الأيقونة
   - ✅ إعادة جلب البيانات

#### زر تغيير نوع المخطط:
1. **مرر الماوس** على مخطط الإيرادات
2. **اضغط على أيقونة** 📊 أو 📈 (Bar/Line/Pie)
3. تحقق من:
   - ✅ تغيير نوع المخطط فوراً

#### زر التصدير:
1. **اضغط على "تصدير PDF"** 📥
2. تحقق من:
   - ✅ فتح نافذة طباعة أو تنزيل PDF

### الخطوة 4: فحص AI Insights
1. ابحث عن **لافتة "رؤى الذكاء الاصطناعي"** (زرقاء)
2. يجب أن تحتوي على:
   - ✅ 3-5 رؤى ذكية مستخرجة من البيانات
   - ✅ أيقونات تعبيرية (💡 🔥 ⭐)
   - ✅ أرقام وإحصائيات محدثة

### الخطوة 5: فحص جدول البيانات
1. **مرر للأسفل** حتى "جدول البيانات التفصيلي"
2. تحقق من:
   - ✅ عرض جميع الدورات
   - ✅ عرض الإيرادات، الطلاب، نسبة الإكمال
   - ✅ إمكانية التصدير إلى CSV

### الخطوة 6: اختبار Console
1. **اضغط F12** لفتح Developer Tools
2. **اذهب إلى تبويب Console**
3. يجب أن **لا ترى**:
   - ❌ أخطاء حمراء (Errors)
   - ❌ تحذيرات من Chart.js
   - ❌ "Failed to fetch" من API

4. يجب أن **ترى**:
   - ✅ `Chart.js initialized`
   - ✅ `Analytics data loaded`
   - ✅ `6 charts rendered`

### النتيجة المتوقعة:
```
✅ جميع المخططات تظهر بشكل صحيح
✅ البيانات تُحمّل من API
✅ الأزرار التفاعلية تعمل
✅ تغيير نوع المخطط يعمل
✅ فلتر الفترة الزمنية يعمل
✅ AI Insights تظهر
✅ لا توجد أخطاء في Console
```

---

## 📊 ملخص نتائج الاختبار

### نموذج تقرير:

```
تاريخ الاختبار: _____________
اسم المختبر: _____________

┌─────────────────────────────┬────────┬────────┬───────────┐
│         النظام              │ يعمل   │ لا يعمل│ ملاحظات    │
├─────────────────────────────┼────────┼────────┼───────────┤
│ 1. نظام الدردشة             │   ☐    │   ☐    │           │
│    - زر الرسائل              │   ☐    │   ☐    │           │
│    - فتح واجهة الدردشة       │   ☐    │   ☐    │           │
│    - إرسال رسالة             │   ☐    │   ☐    │           │
│    - استقبال رسالة           │   ☐    │   ☐    │           │
├─────────────────────────────┼────────┼────────┼───────────┤
│ 2. نظام الإشعارات           │   ☐    │   ☐    │           │
│    - زر الإشعارات           │   ☐    │   ☐    │           │
│    - فتح اللوحة              │   ☐    │   ☐    │           │
│    - قراءة إشعار             │   ☐    │   ☐    │           │
│    - تحديد الكل كمقروء       │   ☐    │   ☐    │           │
│    - حذف إشعار               │   ☐    │   ☐    │           │
├─────────────────────────────┼────────┼────────┼───────────┤
│ 3. نظام الرسوم البيانية     │   ☐    │   ☐    │           │
│    - عرض المخططات            │   ☐    │   ☐    │           │
│    - تغيير نوع المخطط        │   ☐    │   ☐    │           │
│    - فلتر الفترة الزمنية     │   ☐    │   ☐    │           │
│    - زر التحديث              │   ☐    │   ☐    │           │
│    - AI Insights             │   ☐    │   ☐    │           │
│    - التصدير                 │   ☐    │   ☐    │           │
└─────────────────────────────┴────────┴────────┴───────────┘

النسبة الإجمالية: _____ %

المشاكل المكتشفة:
1. _______________________________________________
2. _______________________________________________
3. _______________________________________________

التوصيات:
1. _______________________________________________
2. _______________________________________________
```

---

## 🐛 استكشاف الأخطاء وحلها

### مشكلة: "Table 'messages' doesn't exist"
**الحل:**
```sql
-- قم بتشغيل هذا SQL:
SOURCE database/create_messages_table.sql;
```

### مشكلة: زر الرسائل لا يعمل
**الحل:**
1. افتح Console (F12)
2. ابحث عن أخطاء JavaScript
3. تأكد من تحميل `chat.js`:
```javascript
// يجب أن ترى:
✅ تم تهيئة نظام الرسائل بنجاح
```

### مشكلة: الإشعارات لا تظهر
**الحل:**
1. تأكد من وجود جدول `notifications`
2. تحقق من الصلاحيات (Session)
3. افتح Network tab، تأكد من نجاح `notifications_system.php`

### مشكلة: المخططات لا تظهر
**الحل:**
1. تأكد من تحميل Chart.js:
```javascript
console.log(typeof Chart); // يجب أن يعرض "function"
```
2. تحقق من API:
```
GET http://localhost/Ibdaa-Taiz/Manager/api/get_analytics_data.php
يجب أن يرجع JSON
```

### مشكلة: "NotificationsSystem is not defined"
**الحل:**
```javascript
// تأكد من تحميل notifications.js في dashboard.php:
<script src="Manager/js/notifications.js"></script>
```

---

## ✅ معايير النجاح

يُعتبر الاختبار ناجحاً إذا:
- ✅ 90% أو أكثر من الوظائف تعمل
- ✅ لا توجد أخطاء حرجة في Console
- ✅ جميع الأزرار تستجيب
- ✅ البيانات تُحمّل من API بنجاح
- ✅ Real-time updates تعمل

---

**بالتوفيق في الاختبار! 🚀**

إذا واجهت أي مشكلة، راجع ملف:
`CHAT_NOTIFICATIONS_ANALYTICS_REVIEW_REPORT.md`
