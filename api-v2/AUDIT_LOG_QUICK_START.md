# 🔍 دليل سريع: نظام سجل التدقيق

## ما هو؟
نظام تسجيل تلقائي لجميع العمليات الحساسة في المنصة (حذف، تحديث، إضافة).

## ماذا يسجل؟
- ✅ من قام بالعملية (الاسم، البريد، الصلاحية)
- ✅ ماذا فعل (حذف، تحديث، إضافة)
- ✅ متى (التاريخ والوقت)
- ✅ أين (URL, IP Address)
- ✅ التغييرات (قبل وبعد)

## كيف أستخدمه؟

### 1. مشاهدة جميع السجلات
```bash
GET /api/v1/audit-logs
```

### 2. البحث عن من حذف طالب
```bash
GET /api/v1/audit-logs/model-history?model_type=Student&model_id=123
```

### 3. مشاهدة العمليات الحرجة
```bash
GET /api/v1/audit-logs?severity=critical
```

### 4. تتبع نشاط مستخدم
```bash
GET /api/v1/audit-logs/user/5
```

### 5. إحصائيات شاملة
```bash
GET /api/v1/audit-logs/statistics
```

## الصلاحيات
- ✅ Manager فقط يمكنه الوصول للسجلات
- ✅ التسجيل يتم تلقائياً لجميع المستخدمين

## الملفات الرئيسية
- `app/Models/AuditLog.php` - Model
- `app/Http/Controllers/API/V1/AuditLogController.php` - Controller
- `app/Http/Middleware/AuditLogMiddleware.php` - Middleware
- `AUDIT_LOG_SYSTEM.md` - التوثيق الكامل

## للتفاصيل الكاملة
راجع ملف `AUDIT_LOG_SYSTEM.md`
