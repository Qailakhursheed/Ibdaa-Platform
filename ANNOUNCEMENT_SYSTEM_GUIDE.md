# 🎯 دليل التنفيذ الشامل: نظام الصلاحيات والإعلانات والتجربة المزدوجة

## 📋 نظرة عامة
تم تنفيذ نظام شامل لإدارة الصلاحيات، نشر الإعلانات، وفصل تجربة المستخدم بناءً على حالة التسجيل.

---

## ✅ المراحل المنجزة

### المرحلة 1: تأكيد صلاحيات المشرف الفني ✅
- **الملفات المعدلة:**
  - `Manager/dashboard.php` (الشريط الجانبي)
  - `Manager/api/manage_courses.php`

- **ما تم:**
  - ✅ المشرف الفني (technical) لديه صلاحية كاملة (CRUD) على الدورات
  - ✅ الرابط في الشريط الجانبي: `data-roles="manager,technical"`
  - ✅ API يتحقق من الدور: `in_array($user_role, ['manager','technical'])`

---

### المرحلة 2: بناء مركز تحكم الإعلانات ✅

#### 2.1 قاعدة البيانات
**الملف:** `database/announcements.sql`

```sql
CREATE TABLE IF NOT EXISTS `announcements` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `title` VARCHAR(255) NOT NULL,
  `content` TEXT NOT NULL,
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_created_at` (`created_at` DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**لتطبيق الجدول:**
```bash
# عبر phpMyAdmin أو MySQL CLI
mysql -u root ibdaa_platform < database/announcements.sql
```

#### 2.2 واجهة إدارة الإعلانات
**الملف:** `Manager/dashboard.php`

**التعديلات:**
1. ✅ إضافة رابط في الشريط الجانبي:
   ```html
   <a href="#announcements" class="sidebar-link" data-page="announcements" data-roles="technical">
       <i data-lucide="megaphone"></i><span>إدارة الإعلانات</span>
   </a>
   ```

2. ✅ دالة `renderAnnouncements()` تتضمن:
   - نموذج إضافة إعلان جديد (عنوان + محتوى)
   - جدول يعرض كل الإعلانات مع زر حذف
   - ربط بـ API عبر `fetch()`

3. ✅ دالة `loadAnnouncements()` لجلب وعرض البيانات
4. ✅ دالة `deleteAnnouncement(id)` للحذف

#### 2.3 API إدارة الإعلانات
**الملف:** `Manager/api/manage_announcements.php`

**المميزات:**
- ✅ **GET:** جلب جميع الإعلانات (مرتبة حسب الأحدث)
- ✅ **POST (action=create):** إنشاء إعلان جديد
- ✅ **POST (action=delete):** حذف إعلان
- ✅ **POST (action=update):** تعديل إعلان (اختياري)
- ✅ **الصلاحيات:** فقط المشرف الفني (`$user_role === 'technical'`)

---

### المرحلة 3: نشر الإعلانات على الموقع الخارجي ✅

#### 3.1 تحويل index.html إلى PHP
**الملف:** `platform/index.php` (كان موجوداً بالفعل)

**الإضافات:**
1. ✅ في أعلى الملف (قبل `<!DOCTYPE html>`):
   ```php
   <?php
   require_once 'db.php';
   
   $announcements = [];
   try {
       $stmt = $pdo->query("SELECT * FROM announcements ORDER BY created_at DESC LIMIT 3");
       $announcements = $stmt->fetchAll(PDO::FETCH_ASSOC);
   } catch (Exception $e) {
       $announcements = [];
   }
   ?>
   ```

2. ✅ قسم الإعلانات (بعد قسم الدورات):
   ```html
   <?php if (!empty($announcements)): ?>
   <section id="announcements" class="py-20 bg-white">
       <!-- عرض آخر 3 إعلانات مع تصميم جميل -->
   </section>
   <?php endif; ?>
   ```

**المميزات:**
- بطاقات إعلانات بتصميم gradient
- أيقونات Lucide Icons
- رابط للتسجيل في الدورات
- تاريخ النشر بالتقويم الهجري

---

### المرحلة 4: تجربة الطالب المزدوجة ✅

#### 4.1 منطق التحقق من حالة الطالب
**الملف:** `Manager/dashboard.php` (أعلى ملف PHP)

```php
$is_enrolled_student = false;
$student_announcements = [];

if ($user_role === 'student') {
    // تحقق من التسجيل في دورة نشطة
    $stmt = $pdo->prepare("SELECT COUNT(*) FROM enrollments WHERE user_id = ? AND status = 'active'");
    $stmt->execute([$_SESSION['user_id']]);
    if ($stmt->fetchColumn() > 0) {
        $is_enrolled_student = true;
    }
    
    // جلب الإعلانات للطلاب غير المسجلين
    if (!$is_enrolled_student) {
        $stmt = $pdo->query("SELECT * FROM announcements ORDER BY created_at DESC LIMIT 5");
        $student_announcements = $stmt->fetchAll(PDO::FETCH_ASSOC);
    }
}
```

#### 4.2 فصل الواجهات (HTML)

**1. إخفاء الشريط الجانبي للطلاب:**
```php
<?php if ($user_role !== 'student'): ?>
    <aside class="w-64 ...">
        <!-- الشريط الجانبي -->
    </aside>
<?php endif; ?>
```

**2. ثلاث واجهات مختلفة:**

##### أ. الطالب غير المسجل:
```php
<?php if ($user_role === 'student' && !$is_enrolled_student): ?>
    <div class="flex-1 bg-gradient-to-br from-blue-50 to-indigo-100">
        <!-- رسالة ترحيبية -->
        <!-- زر العودة للصفحة الرئيسية -->
        <!-- زر تصفح الدورات -->
        <!-- قسم الإعلانات -->
    </div>
<?php endif; ?>
```

**المميزات:**
- ✅ رسالة ترحيبية كبيرة
- ✅ أيقونة `sparkles` من Lucide
- ✅ زرين: "العودة للصفحة الرئيسية" و "تصفح الدورات"
- ✅ عرض آخر 5 إعلانات في بطاقات جميلة

##### ب. الطالب المسجل:
```php
<?php elseif ($user_role === 'student' && $is_enrolled_student): ?>
    <div class="flex-1 overflow-y-auto bg-gray-50">
        <!-- هيدر خاص بالطالب -->
        <!-- 3 بطاقات: دوراتي - درجاتي - بطاقتي -->
        <!-- قسم النشاطات -->
    </div>
<?php endif; ?>
```

**المميزات:**
- ✅ هيدر مخصص بدون شريط جانبي
- ✅ 3 بطاقات بألوان مختلفة (أزرق، أخضر، بنفسجي)
- ✅ زر "عرض البطاقة" يفتح `api/generate_id_card.php`
- ✅ قسم "نشاطاتي الأخيرة" (جاهز للتطوير)

##### ج. المدراء والمشرفين والمدربين:
```php
<?php else: ?>
    <main class="flex-1 overflow-y-auto">
        <!-- الهيدر العادي -->
        <!-- المحتوى الديناميكي (app-content) -->
    </main>
<?php endif; ?>
```

---

## 🚀 خطوات التفعيل

### 1. تطبيق قاعدة البيانات
```bash
# عبر phpMyAdmin
# 1. افتح phpMyAdmin
# 2. اختر قاعدة البيانات ibdaa_platform
# 3. اذهب إلى تبويب SQL
# 4. انسخ محتوى database/announcements.sql والصقه
# 5. اضغط Go
```

**أو عبر Terminal:**
```bash
cd c:\xampp\mysql\bin
.\mysql -u root ibdaa_platform < "c:\xampp\htdocs\Ibdaa-Taiz\database\announcements.sql"
```

### 2. اختبار النظام

#### أ. اختبار مركز الإعلانات (المشرف الفني):
1. سجل دخول كمشرف فني (technical)
2. انقر على "إدارة الإعلانات" في الشريط الجانبي
3. أضف إعلاناً جديداً
4. تأكد من ظهوره في الجدول
5. جرب حذف إعلان

#### ب. اختبار الموقع الخارجي:
1. افتح `http://localhost/Ibdaa-Taiz/platform/index.php`
2. تأكد من ظهور قسم "آخر الإعلانات" بعد قسم الدورات
3. يجب أن تظهر آخر 3 إعلانات فقط

#### ج. اختبار تجربة الطالب:
**الطالب غير المسجل:**
1. سجل دخول كطالب ليس له تسجيل في أي دورة
2. يجب أن ترى:
   - لا يوجد شريط جانبي
   - رسالة ترحيبية كبيرة
   - زر "العودة للصفحة الرئيسية"
   - زر "تصفح الدورات"
   - قسم الإعلانات (آخر 5)

**الطالب المسجل:**
1. سجل دخول كطالب مسجل في دورة نشطة
2. يجب أن ترى:
   - لا يوجد شريط جانبي
   - هيدر بسيط
   - 3 بطاقات: "دوراتي"، "درجاتي"، "بطاقتي"
   - قسم "نشاطاتي الأخيرة"

---

## 📁 الملفات المعدلة

### ملفات جديدة:
1. ✅ `database/announcements.sql` - جدول الإعلانات
2. ✅ `ANNOUNCEMENT_SYSTEM_GUIDE.md` - هذا الملف

### ملفات معدلة:
1. ✅ `Manager/dashboard.php` - إضافات كبيرة:
   - منطق التحقق من حالة الطالب (PHP)
   - رابط "إدارة الإعلانات" في الشريط الجانبي
   - دالة `renderAnnouncements()` + `loadAnnouncements()` + `deleteAnnouncement()`
   - فصل الواجهات (3 حالات مختلفة)

2. ✅ `Manager/api/manage_announcements.php` - تحديث شامل:
   - استخدام PDO بدلاً من MySQLi
   - تحسين الصلاحيات (technical فقط)
   - دعم JSON input

3. ✅ `platform/index.php` - إضافات:
   - كود PHP لجلب الإعلانات
   - قسم HTML لعرض الإعلانات

---

## 🎨 التصميم والـ UX

### الألوان المستخدمة:
- **الإعلانات:** Indigo/Blue Gradient
- **بطاقة الدورات:** Blue (bg-blue-600)
- **بطاقة الدرجات:** Green (bg-green-600)
- **بطاقة الهوية:** Purple (bg-purple-600)

### الأيقونات (Lucide Icons):
- `megaphone` - إدارة الإعلانات
- `bell` - إشعارات الإعلانات
- `sparkles` - شعار المنصة
- `book-open` - الدورات
- `award` - الدرجات
- `credit-card` - البطاقة
- `calendar` - التاريخ

---

## 🔐 نظام الصلاحيات

| الدور | الشريط الجانبي | إدارة الإعلانات | عرض الإعلانات | الواجهة |
|------|----------------|-----------------|---------------|---------|
| **manager** | ✅ كامل | ❌ | - | لوحة تحكم كاملة |
| **technical** | ✅ كامل | ✅ | - | لوحة تحكم كاملة |
| **trainer** | ✅ محدود | ❌ | - | لوحة تحكم محدودة |
| **student (غير مسجل)** | ❌ | ❌ | ✅ (5 إعلانات) | صفحة ترحيب + إعلانات |
| **student (مسجل)** | ❌ | ❌ | ❌ | لوحة الطالب الكاملة |
| **زائر (غير مسجل)** | ❌ | ❌ | ✅ (3 إعلانات) | الموقع الخارجي |

---

## 📊 قاعدة البيانات

### جدول enrollments (مطلوب للتحقق):
```sql
-- تأكد من وجود هذا الجدول
SHOW TABLES LIKE 'enrollments';

-- الأعمدة المطلوبة:
-- user_id, course_id, status ('active', 'completed', 'pending')
```

### اختبار بيانات وهمية:
```sql
-- إضافة تسجيل وهمي لاختبار الطالب المسجل
INSERT INTO enrollments (user_id, course_id, status) 
VALUES (1, 1, 'active');

-- حذف التسجيل لاختبار الطالب غير المسجل
DELETE FROM enrollments WHERE user_id = 1;
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: الإعلانات لا تظهر
**الحل:**
```php
// تحقق من أن جدول announcements موجود
SHOW TABLES LIKE 'announcements';

// تحقق من البيانات
SELECT * FROM announcements;
```

### مشكلة: الطالب يرى الشريط الجانبي
**الحل:**
```php
// تحقق من قيمة $user_role
var_dump($user_role); // يجب أن تكون 'student'

// تحقق من الشرط في dashboard.php
<?php if ($user_role !== 'student'): ?>
```

### مشكلة: خطأ "table enrollments doesn't exist"
**الحل:**
```sql
-- إنشاء الجدول إذا لم يكن موجوداً
CREATE TABLE IF NOT EXISTS enrollments (
    enrollment_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    course_id INT NOT NULL,
    status ENUM('pending', 'active', 'completed', 'cancelled') DEFAULT 'pending',
    enrolled_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (course_id) REFERENCES courses(course_id)
);
```

---

## 🎯 الخطوات التالية (اختياري)

1. **تطوير لوحة الطالب المسجل:**
   - عرض الدورات الفعلية المسجل عليها
   - عرض الدرجات من جدول `grades`
   - إضافة قسم "الواجبات"

2. **تحسين نظام الإعلانات:**
   - إضافة زر "تعديل" للإعلانات
   - إمكانية ربط الإعلان بدورة محددة
   - إضافة صورة للإعلان

3. **إشعارات في الوقت الفعلي:**
   - استخدام WebSockets لإشعارات فورية
   - عداد الإعلانات غير المقروءة

---

## 📞 الدعم الفني

للمساعدة أو الاستفسارات:
- **المطور:** RoboStack-Yemen
- **الموقع:** منصة إبداع - تعز، اليمن

---

## ✅ ملخص الإنجاز

تم بنجاح:
1. ✅ تأكيد صلاحيات المشرف الفني على الدورات
2. ✅ بناء مركز تحكم الإعلانات (للمشرف الفني)
3. ✅ نشر الإعلانات على الموقع الخارجي (index.php)
4. ✅ فصل تجربة الطالب إلى مسارين:
   - طالب غير مسجل: صفحة ترحيب + إعلانات
   - طالب مسجل: لوحة تحكم كاملة

**النظام جاهز للاستخدام! 🎉**
