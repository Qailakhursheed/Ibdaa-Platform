# 🚀 دليل سريع: تفعيل نظام الإحصائيات والمالية

## خطوة 1: تطبيق قاعدة البيانات

افتح **phpMyAdmin** واختر قاعدة `ibdaa_platform`:

```sql
-- نفذ ملف المدفوعات
SOURCE C:/xampp/htdocs/Ibdaa-Taiz/database/payments.sql;
```

أو:
1. افتح phpMyAdmin
2. اختر قاعدة `ibdaa_platform`
3. اذهب إلى تبويب "SQL"
4. افتح ملف `database/payments.sql` وانسخ محتواه والصقه
5. اضغط "Go"

---

## خطوة 2: اختبار API الإحصائيات

افتح في المتصفح:
```
http://localhost/Ibdaa-Taiz/Manager/api/get_dashboard_stats.php
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "stats": {
    "total_trainees": 245,
    "active_courses": 18,
    "total_revenue": 2500.00,
    "certs_issued": 0
  }
}
```

---

## خطوة 3: اختبار API المالية

**شرط**: يجب تسجيل الدخول بحساب `role='technical'`

افتح في المتصفح:
```
http://localhost/Ibdaa-Taiz/Manager/api/manage_finance.php
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": [
    {
      "payment_id": 1,
      "amount": "500.00",
      "payment_method": "cash",
      "status": "completed",
      "student_name": "محمد علي أحمد",
      "course_title": "ICDL - الرخصة الدولية"
    }
  ],
  "summary": {
    "total_revenue": "2500.00",
    "total_payments": "5",
    "avg_payment": "500.00"
  }
}
```

---

## خطوة 4: اختبار لوحة التحكم

1. افتح: `http://localhost/Ibdaa-Taiz/Manager/login.php`
2. سجل دخول بحساب دعم فني (technical)
3. ستظهر لوحة التحكم بالإحصائيات الحقيقية
4. اضغط على "الشؤون المالية" في القائمة الجانبية
5. يجب أن تشاهد:
   - 3 بطاقات إحصائيات (إجمالي الإيرادات، عدد الدفعات، متوسط الدفعة)
   - رسم بياني للإيرادات الشهرية
   - جدول الدفعات
   - زر "إضافة دفعة جديدة"

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "لا توجد دفعات مسجلة"
**السبب**: لم يتم تطبيق ملف `payments.sql`
**الحل**: نفذ الخطوة 1 مرة أخرى

### المشكلة 2: "غير مصرح لك - هذا القسم للدعم الفني فقط"
**السبب**: تسجيل الدخول بحساب غير technical
**الحل**: سجل دخول بحساب role='technical' أو أنشئ واحد:
```sql
INSERT INTO users (full_name, email, password, role) 
VALUES ('دعم فني', 'technical@ibdaa.com', '$2y$10$...', 'technical');
```

### المشكلة 3: الرسم البياني لا يظهر
**السبب**: Chart.js لم يتم تحميله
**الحل**: تأكد من وجود السطر التالي في `<head>`:
```html
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
```

### المشكلة 4: الإحصائيات تظهر 0 أو null
**السبب**: لا توجد بيانات في الجداول
**الحل**: 
- أضف طلاب: `INSERT INTO users (role='student')`
- أضف دورات: `INSERT INTO courses (status='active')`
- أضف دفعات: `INSERT INTO payments (status='completed')`

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل، تحقق من:
1. Console المتصفح (F12) → هل هناك أخطاء JavaScript؟
2. Network Tab (F12) → هل API تعيد استجابات صحيحة؟
3. ملفات PHP error logs

---

© 2024 منصة إبداع للتدريب
