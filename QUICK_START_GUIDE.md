# 🚀 دليل الاستخدام السريع - منصة إبداع

## 📦 التثبيت والإعداد

### الخطوة 1: استيراد قاعدة البيانات
```sql
-- افتح PHPMyAdmin
-- اختر قاعدة بيانات المنصة
-- استورد الملف التالي:
database/support_system.sql
```

### الخطوة 2: إعداد البريد الإلكتروني
افتح `platform/support.php` وعدّل:
```php
define('SUPPORT_EMAIL', 'support@ibdaa-platform.com'); // بريدك
define('ADMIN_EMAIL', 'admin@ibdaa-platform.com');    // بريد المدير
```

### الخطوة 3: تشغيل XAMPP
```
✅ Apache
✅ MySQL
```

---

## 🎯 الميزات الجديدة

### 1️⃣ نظام الدعم الفني

**الرابط:** `http://localhost/Ibdaa-Taiz/platform/support.php`

**كيف تستخدمه:**
1. املأ النموذج بالبيانات
2. اختر الفئة والأولوية
3. اضغط "إرسال التذكرة"
4. احصل على رقم التذكرة
5. سيصل بريد تأكيد تلقائياً

**تتبع التذكرة:**
- اذهب إلى: `http://localhost/Ibdaa-Taiz/platform/track_ticket.php`
- أدخل رقم التذكرة
- شاهد الحالة والردود

---

### 2️⃣ صفحة الدورات المحسنة

**الرابط:** `http://localhost/Ibdaa-Taiz/platform/courses.html`

**المميزات:**
- 🔍 **بحث ذكي:** ابحث عن أي دورة
- 🏷️ **فلترة:** صنف حسب المجال (ICDL، برمجة، تحليل بيانات، إلخ)
- 💧 **علامة مائية:** شعار المنصة على كل صورة
- ✨ **تأثيرات:** حركات سلسة عند التمرير

**كيف تستخدمها:**
1. اكتب في مربع البحث (مثال: "Excel")
2. أو اضغط على فلتر (مثال: "تحليل بيانات")
3. اضغط على أي دورة للتفاصيل
4. اضغط "التسجيل" للتقديم

---

### 3️⃣ نظام الإعلانات

**الرابط:** `http://localhost/Ibdaa-Taiz/platform/announcements.html`

**الأنواع:**
- 🔴 **مهم:** إعلانات عاجلة
- 📅 **فعاليات:** أنشطة وأحداث
- 📚 **دورات:** دورات جديدة
- 🗞️ **أخبار:** إنجازات وأخبار
- 🎁 **عروض:** خصومات

**كيف تستخدمه:**
1. اختر فلتر (الكل، مهم، فعاليات، إلخ)
2. اضغط "عرض التفاصيل" لأي إعلان
3. اقرأ التفاصيل الكاملة
4. اضغط "سجل الآن" للتسجيل

---

### 4️⃣ نظام العلامة المائية

**الملف:** `platform/watermark_system.php`

#### **إضافة علامة على صورة واحدة:**
```php
<?php
require_once 'watermark_system.php';

$watermark = new WatermarkManager();

// صورة واحدة
$result = $watermark->addWatermark(
    'photos/course1.jpg',  // الصورة الأصلية
    'photos/course1.jpg',  // نفس المسار = استبدال
    [
        'opacity' => 30,          // الشفافية
        'position' => 'bottom-right', // الموقع
        'size' => 15,             // الحجم (%)
        'padding' => 20           // المسافة من الحافة
    ]
);

if ($result['success']) {
    echo "تم إضافة العلامة المائية!";
} else {
    echo "خطأ: " . $result['error'];
}
?>
```

#### **معالجة مجلد كامل:**
```php
<?php
require_once 'watermark_system.php';

$watermark = new WatermarkManager();

// مجلد كامل
$result = $watermark->processDirectory(
    'photos/',              // المجلد الأصلي
    'photos/watermarked/'  // مجلد الحفظ (اختياري)
);

echo "تمت معالجة: " . $result['processed'] . " صورة";
echo "فشلت: " . $result['failed'] . " صورة";
?>
```

#### **علامة مائية نصية:**
```php
<?php
$result = $watermark->addTextWatermark(
    'photos/image.jpg',
    '© منصة إبداع 2025',
    null,
    [
        'fontSize' => 20,
        'fontColor' => [255, 255, 255], // أبيض
        'opacity' => 40,
        'position' => 'bottom-center'
    ]
);
?>
```

#### **مواقع العلامة المائية:**
- `center` - الوسط
- `top-left` - أعلى يسار
- `top-right` - أعلى يمين
- `bottom-left` - أسفل يسار
- `bottom-right` - أسفل يمين (الافتراضي)

---

## 🎨 تخصيص التصميم

### تغيير الألوان الأساسية:

في أي ملف HTML، ابحث عن:
```css
/* اللون الأساسي */
.bg-indigo-600  → غيّره إلى .bg-purple-600
.text-indigo-600 → غيّره إلى .text-purple-600

/* اللون الثانوي */
.bg-blue-600 → غيّره إلى .bg-cyan-600
```

### إضافة دورة جديدة:

في `courses.html`، انسخ والصق:
```html
<div class="course-card bg-gray-50 rounded-2xl overflow-hidden border shadow-lg" 
     data-category="programming" 
     data-keywords="كلمات مفتاحية للبحث">
  <div class="relative overflow-hidden h-48">
    <img src="رابط_الصورة" class="w-full h-full object-cover" alt="اسم الدورة" />
    <img src="photos/Sh.jpg" alt="شعار إبداع" class="watermark rounded-full">
    <span class="badge absolute top-4 left-4 bg-green-400 text-green-900 px-3 py-1 rounded-full text-sm font-bold">
      🆕 جديد
    </span>
  </div>
  <div class="p-6 relative z-10">
    <h3 class="text-2xl font-bold text-indigo-700 mb-3">عنوان الدورة</h3>
    <p class="text-gray-700 mb-4">وصف مختصر للدورة...</p>
    <ul class="list-disc text-gray-600 ms-6">
      <li>نقطة 1</li>
      <li>نقطة 2</li>
      <li>نقطة 3</li>
    </ul>
    <a href="application.php?course=اسم_الدورة" 
       class="inline-block mt-4 bg-indigo-600 text-white px-5 py-2 rounded-lg shadow hover:bg-indigo-700 transition">
      التسجيل في هذه الدورة
    </a>
  </div>
</div>
```

### إضافة إعلان جديد:

في `announcements.html`، انسخ والصق بطاقة إعلان موجودة وعدّل:
- اللون (من `from-red-500` إلى لون آخر)
- الأيقونة (غيّر `data-lucide="icon-name"`)
- النص والمحتوى
- `data-category` للفلترة

---

## 🔧 الصيانة

### مشاكل شائعة وحلولها:

#### **1. البريد لا يُرسَل**

**الحل:**
```php
// في support.php، استخدم PHPMailer بدلاً من mail()
require 'PHPMailer/PHPMailer.php';
require 'PHPMailer/SMTP.php';

$mail = new PHPMailer();
$mail->isSMTP();
$mail->Host = 'smtp.gmail.com';
$mail->SMTPAuth = true;
$mail->Username = 'your-email@gmail.com';
$mail->Password = 'your-password';
$mail->SMTPSecure = 'tls';
$mail->Port = 587;
$mail->setFrom('your-email@gmail.com', 'منصة إبداع');
$mail->addAddress($email, $name);
$mail->Subject = $user_subject;
$mail->msgHTML($user_message);
$mail->send();
```

#### **2. الصور لا تظهر**

**تحقق من:**
- المسار صحيح: `photos/Sh.jpg`
- الملف موجود
- صلاحيات المجلد (755)

#### **3. العلامة المائية لا تضاف**

**تحقق من:**
- GD library مثبتة: `phpinfo()` ابحث عن "GD"
- صلاحيات الكتابة على المجلد
- صيغة الصورة مدعومة (JPG, PNG, GIF)

#### **4. الفلترة لا تعمل**

**تحقق من:**
- JavaScript مفعّل في المتصفح
- لا يوجد أخطاء في Console (F12)
- `data-category` و `data-keywords` موجودة

---

## 📱 التواصل

### الروابط المهمة:
- 🏠 **الرئيسية:** `index.html`
- 📚 **الدورات:** `courses.html`
- 📢 **الإعلانات:** `announcements.html`
- 🎧 **الدعم الفني:** `support.php`
- 🔍 **تتبع التذكرة:** `track_ticket.php`
- 🧪 **اختبار API:** `test_api_key.html`

### في لوحة التحكم:
- **للمدير:** `Manager/index.html`
- **الدعم الفني:** `Manager/dashboards/technical/support.php`

---

## ✅ قائمة التحقق قبل الإطلاق

- [ ] استيراد قاعدة البيانات
- [ ] تحديث بيانات البريد الإلكتروني
- [ ] اختبار نموذج الدعم
- [ ] إضافة علامة مائية على جميع الصور
- [ ] مراجعة محتوى الدورات
- [ ] تحديث معلومات الاتصال
- [ ] اختبار على الموبايل
- [ ] اختبار على متصفحات مختلفة
- [ ] تفعيل HTTPS
- [ ] نسخ احتياطي للبيانات

---

## 🎉 جاهز للانطلاق!

الآن المنصة جاهزة بالكامل مع:
- ✅ نظام دعم فني متكامل
- ✅ تصميم حديث ومتقدم
- ✅ علامة مائية على جميع الصور
- ✅ بحث وفلترة ذكية
- ✅ نظام إعلانات احترافي
- ✅ تجربة مستخدم سهلة وفعالة

**بالتوفيق! 🚀**

---

© 2025 منصة إبداع للتدريب والتأهيل
