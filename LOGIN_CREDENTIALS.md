# 🔐 بيانات الدخول الحقيقية
## Real Login Credentials

---

## 📋 بيانات المستخدمين الافتراضية

### 1️⃣ المدير (Manager)
- **الاسم:** أحمد محمد الإبداع
- **البريد الإلكتروني:** `manager@ibdaa.edu.ye`
- **كلمة المرور:** `Ibdaa@Manager2024`
- **الهاتف:** +967777000001
- **الصلاحيات:** 
  - إدارة كاملة للنظام
  - إضافة/تعديل/حذف المستخدمين
  - إدارة الدورات والمدفوعات
  - عرض التقارير والإحصائيات
  - إدارة الإشعارات والدردشة

---

### 2️⃣ المشرف الفني (Technical Supervisor)
- **الاسم:** خالد عبدالله التقني
- **البريد الإلكتروني:** `technical@ibdaa.edu.ye`
- **كلمة المرور:** `Ibdaa@Tech2024`
- **الهاتف:** +967777000002
- **الصلاحيات:**
  - مراجعة الدورات
  - تقييم المدربين
  - إدارة الدعم الفني
  - عرض التقارير التقنية

---

### 3️⃣ المدرب (Trainer)
- **الاسم:** فاطمة أحمد المدربة
- **البريد الإلكتروني:** `trainer@ibdaa.edu.ye`
- **كلمة المرور:** `Ibdaa@Trainer2024`
- **الهاتف:** +967777000003
- **الصلاحيات:**
  - عرض قائمة الطلاب المسجلين
  - تسجيل الحضور والغياب
  - رفع المواد التدريبية
  - إدخال الدرجات
  - التواصل مع الطلاب

---

### 4️⃣ الطالب (Student)
- **الاسم:** محمد سعيد الطالب
- **البريد الإلكتروني:** `student@ibdaa.edu.ye`
- **كلمة المرور:** `Ibdaa@Student2024`
- **الهاتف:** +967777000004
- **الصلاحيات:**
  - عرض الدورات المسجل بها
  - تنزيل البطاقة الشخصية
  - عرض الحالة المالية
  - استلام الإشعارات
  - التواصل مع المدربين

---

### 5️⃣ طلاب إضافيون (للاختبار)

**الطالبة نور:**
- **البريد:** `noor.ali@ibdaa.edu.ye`
- **كلمة المرور:** `Ibdaa@Student2024`

**الطالب عبدالرحمن:**
- **البريد:** `abdulrahman.hassan@ibdaa.edu.ye`
- **كلمة المرور:** `Ibdaa@Student2024`

---

## 🚀 خطوات تفعيل النظام

### الخطوة 1: تشغيل XAMPP
```bash
# تأكد من تشغيل Apache و MySQL
```

### الخطوة 2: تنفيذ ملف SQL
افتح phpMyAdmin: `http://localhost/phpmyadmin`

**الطريقة الأولى (phpMyAdmin):**
1. اختر قاعدة البيانات `ibdaa_platform`
2. انقر على تبويب "SQL"
3. افتح الملف `database/initial_users.sql`
4. انسخ المحتوى والصقه
5. اضغط "تنفيذ" (Execute)

**الطريقة الثانية (Terminal):**
```powershell
cd C:\xampp\htdocs\Ibdaa-Taiz
C:\xampp\mysql\bin\mysql.exe -u root ibdaa_platform < database\initial_users.sql
```

### الخطوة 3: اختبار الدخول
افتح المتصفح: `http://localhost/Ibdaa-Taiz/Manager/login.php`

---

## 🔄 آلية الدخول (Login Flow)

```
1. المستخدم يفتح: login.php
                    ↓
2. يدخل البريد وكلمة المرور
                    ↓
3. النظام يتحقق من قاعدة البيانات
                    ↓
4. التحقق من كلمة المرور باستخدام password_verify()
                    ↓
5. إنشاء جلسة (Session) وحفظ:
   - user_id
   - user_name
   - user_email
   - user_role
   - user_photo
                    ↓
6. توجيه تلقائي حسب الدور:
   
   manager     → dashboards/manager-dashboard.php
   technical   → dashboards/technical-dashboard.php
   trainer     → dashboards/trainer-dashboard.php
   student     → dashboards/student-dashboard.php
                    ↓
7. اللوحة تتحقق من الجلسة في shared-header.php
                    ↓
8. إذا لم تكن هناك جلسة → العودة لـ login.php
```

---

## 🔒 الأمان (Security)

### كلمات المرور المشفرة:
- **الخوارزمية:** bcrypt (password_hash)
- **التكلفة:** 10 (قوة التشفير)
- **التحقق:** password_verify()

### حماية الجلسات:
```php
session_start();
if (!isset($_SESSION['user_id'])) {
    header('Location: ../login.php');
    exit;
}
```

### حماية من SQL Injection:
```php
$stmt = $conn->prepare("SELECT * FROM users WHERE email = ?");
$stmt->bind_param("s", $email);
```

---

## 📝 ملاحظات مهمة

1. **تغيير كلمات المرور:**
   - بعد أول تسجيل دخول، يُنصح بتغيير كلمة المرور
   - يمكن إضافة صفحة "تغيير كلمة المرور" في الإعدادات

2. **البريد الإلكتروني:**
   - استخدمنا نطاق `@ibdaa.edu.ye` (تعليمي)
   - يمكن تغييره لأي نطاق آخر

3. **إضافة مستخدمين جدد:**
   - من لوحة المدير → إدارة المستخدمين → إضافة مستخدم جديد
   - أو تشغيل SQL مباشرة

4. **استرجاع كلمة المرور:**
   - حالياً غير متاح
   - يمكن إضافة هذه الميزة لاحقاً عبر البريد الإلكتروني

---

## 🧪 اختبار النظام

### 1. اختبار تسجيل الدخول:
```
http://localhost/Ibdaa-Taiz/Manager/test-login.php
```

### 2. تسجيل دخول مباشر:
```
http://localhost/Ibdaa-Taiz/Manager/login.php
```

### 3. لوحة المدير:
```
http://localhost/Ibdaa-Taiz/Manager/dashboards/manager-dashboard.php
```

---

## 🆘 حل المشاكل

### المشكلة: "Access denied for user"
**الحل:** تحقق من إعدادات MySQL في `database/db.php`

### المشكلة: "Session not working"
**الحل:** تأكد من أن `session_start()` موجود في `shared-header.php`

### المشكلة: "Invalid credentials"
**الحل:** تأكد من تنفيذ `initial_users.sql` بشكل صحيح

### المشكلة: "Redirect loop"
**الحل:** امسح الكوكيز والكاش من المتصفح

---

## 📞 الدعم

إذا واجهت أي مشاكل، تحقق من:
1. ✅ Apache يعمل (port 80)
2. ✅ MySQL يعمل (port 3306)
3. ✅ قاعدة البيانات `ibdaa_platform` موجودة
4. ✅ جدول `users` يحتوي على بيانات
5. ✅ ملف `database/db.php` يحتوي على إعدادات صحيحة

---

**تم إنشاء هذا الملف بتاريخ:** 2024-11-10  
**النظام:** منصة إبداع التعليمية - Ibdaa Educational Platform
