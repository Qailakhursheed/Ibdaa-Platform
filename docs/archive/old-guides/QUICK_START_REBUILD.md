# ⚡ دليل الخطوات السريعة

## 🔧 الإعداد الأولي (SQL)

قم بتنفيذ هذه الملفات في قاعدة البيانات بالترتيب:

```sql
-- 1. جدول الإشعارات
SOURCE database/notifications.sql;

-- 2. جدول الإعدادات
SOURCE database/settings.sql;

-- 3. تحديث enrollments
SOURCE database/update_enrollments_payment.sql;
```

أو من phpMyAdmin: استيراد كل ملف SQL

---

## 📧 إعداد البريد الإلكتروني (ضروري جداً!)

### لـ Gmail:
1. اذهب إلى: https://myaccount.google.com/security
2. فعّل "التحقق بخطوتين"
3. اذهب إلى "كلمات مرور التطبيقات": https://myaccount.google.com/apppasswords
4. اختر "تطبيق آخر" وأدخل "منصة إبداع"
5. انسخ كلمة المرور المكونة من 16 حرف

### تخزين الإعدادات:
1. سجّل دخول كـ **manager**
2. اذهب إلى "الإعدادات"
3. أدخل:
   - SMTP Host: `smtp.gmail.com`
   - SMTP Port: `587`
   - SMTP User: `your-email@gmail.com`
   - SMTP Pass: `كلمة مرور التطبيق (16 حرف)`
   - WhatsApp Number: `967xxxxxxxxx`
4. احفظ

---

## 🧪 اختبار النظام

### 1. اختبار الإشعارات:
- افتح console المتصفح (F12)
- انتظر 30 ثانية
- راقب طلبات API: `get_notifications.php`
- يجب أن تظهر النقطة الحمراء إذا كانت هناك إشعارات

### 2. اختبار تدفق الطلبات:
1. كطالب: قدّم طلب تسجيل من `platform/apply.php`
2. كمشرف فني: افتح "طلبات التسجيل"
3. اضغط "موافقة" → يجب أن يُرسل إيميل للطالب
4. اذهب إلى "الشؤون المالية"
5. اضغط "تأكيد الدفع" → يجب أن يُرسل إيميل التفعيل

### 3. اختبار البريد:
- راقب Console → Network → `send_communication.php`
- إذا فشل: تحقق من إعدادات SMTP
- تحقق من `allow_url_fopen = On` في php.ini

---

## 🚨 حل المشاكل الشائعة

### ❌ الإيميلات لا تُرسل
```php
// تأكد من إعدادات SMTP في جدول settings:
SELECT * FROM settings WHERE setting_key LIKE 'smtp_%';

// تحقق من allow_url_fopen:
<?php echo ini_get('allow_url_fopen') ? 'مفعّل' : 'معطّل'; ?>
```

### ❌ payment_status غير موجود
```sql
-- أضفه يدوياً:
ALTER TABLE enrollments ADD COLUMN payment_status ENUM('pending','paid') DEFAULT 'pending';
```

### ❌ الإشعارات لا تظهر
- افتح F12 → Console
- ابحث عن أخطاء JavaScript
- تحقق من وجود جدول notifications

---

## 📋 المهام المتبقية (للإكمال)

### ✅ تم إنجازه:
- ✅ إعادة هيكلة الصلاحيات
- ✅ نظام الإشعارات الفورية
- ✅ ربط الموافقة بالإيميل
- ✅ ربط الدفع بالتفعيل
- ✅ PHPMailer مثبت
- ✅ جداول SQL منشأة
- ✅ APIs جاهزة

### 🔄 يحتاج عمل يدوي:
1. **renderSettings()** - إنشاء واجهة الإعدادات في dashboard.php
2. **renderFinance() محدّث** - إضافة زر "تأكيد الدفع" وربطه بAPI
3. **renderRequests() محدّث** - إضافة Modal لإدخال سبب الرفض
4. **renderAnnouncements()** - إنشاء واجهة CRUD للإعلانات
5. **platform/index.html → .php** - جلب وعرض الإعلانات للزوار

---

## 💡 نصائح مهمة

1. **للتطوير**: استخدم بريد Gmail حقيقي
2. **للإنتاج**: استخدم SMTP مخصص للشركة
3. **الأمان**: لا تشارك كلمات مرور التطبيقات أبداً
4. **الاختبار**: ابدأ بطلب تسجيل واحد ثم تابع التدفق كاملاً

---

## 📞 الدعم

عند مواجهة مشكلة:
1. افتح Console (F12)
2. اذهب إلى Network → فلتر XHR
3. ابحث عن الطلبات الفاشلة (حمراء)
4. انقر عليها → Preview/Response
5. اقرأ رسالة الخطأ

**جاهز للانطلاق! 🚀**
