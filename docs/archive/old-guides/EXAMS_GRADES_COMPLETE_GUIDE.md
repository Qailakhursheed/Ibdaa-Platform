# 🎓 دليل الإعداد الكامل - نظام الاختبارات والدرجات

## 📋 المحتويات
1. [نظرة عامة](#overview)
2. [متطلبات النظام](#requirements)
3. [تثبيت قاعدة البيانات](#database-setup)
4. [إدخال البيانات التجريبية](#test-data)
5. [دليل الاستخدام](#usage-guide)
6. [الواجهات المتاحة](#interfaces)
7. [نظام منع الغش](#anti-cheat)
8. [التكامل مع الشهادات](#certificates)

---

## 🎯 نظرة عامة {#overview}

نظام شامل ومتكامل لإدارة الاختبارات والدرجات مع ميزات متقدمة:

### ✨ الميزات الأساسية:
- ✅ **إنشاء اختبارات متنوعة** (اختيار متعدد، صح/خطأ، أسئلة مقالية)
- ✅ **تصحيح تلقائي** للأسئلة الموضوعية
- ✅ **نظام درجات ديناميكي** مع أوزان قابلة للتخصيص
- ✅ **إصدار شهادات تلقائي** عند اجتياز الدورة
- ✅ **نظام منع غش متقدم** (10+ طريقة كشف)
- ✅ **تحديثات لحظية** للدرجات للطلاب
- ✅ **إشعارات فورية** عند إدخال/تحديث الدرجات
- ✅ **صلاحيات متعددة** (مدير، تقني، مدرب، طالب)

---

## 💻 متطلبات النظام {#requirements}

### متطلبات الخادم:
```
✅ PHP 7.4+ (مع تفعيل GD Extension)
✅ MySQL 5.7+ أو MariaDB 10.3+
✅ Apache/Nginx
✅ XAMPP/WAMP (للتطوير المحلي)
```

### متطلبات المتصفح:
```
✅ Chrome 90+
✅ Firefox 88+
✅ Safari 14+
✅ Edge 90+
```

### الامتدادات المطلوبة:
```php
extension=gd          // لمعالجة الصور
extension=mysqli      // للاتصال بقاعدة البيانات
extension=pdo_mysql   // بديل للـ mysqli
```

---

## 🗄️ تثبيت قاعدة البيانات {#database-setup}

### الخطوة 1: تشغيل ملف Schema الرئيسي

افتح **phpMyAdmin** (`http://localhost/phpmyadmin`):

**الطريقة الأولى - استيراد مباشر:**
1. اذهب إلى تبويب **Import**
2. اضغط **Choose File**
3. اختر `database/exams_grades_schema.sql`
4. اضغط **Go**

**الطريقة الثانية - نسخ ولصق:**
1. افتح `database/exams_grades_schema.sql` في محرر نصوص
2. انسخ المحتوى كاملاً
3. في phpMyAdmin، اذهب إلى تبويب **SQL**
4. الصق المحتوى
5. اضغط **Go**

هذا الملف سيقوم بإنشاء:
- ✅ 6 جداول جديدة (exams, exam_questions, exam_attempts, ...)
- ✅ 2 عرض (Views) للتقارير
- ✅ Triggers للإشعارات التلقائية
- ✅ Indexes لتحسين الأداء

### الخطوة 2: التحقق من التثبيت

قم بتشغيل هذا الاستعلام للتحقق:

```sql
SHOW TABLES LIKE 'exam%';
SHOW TABLES LIKE 'student_grades';
```

يجب أن تظهر النتيجة:
```
exams
exam_questions
exam_attempts
exam_answers
exam_anti_cheat_log
student_grades
```

---

## 🧪 إدخال البيانات التجريبية {#test-data}

### تشغيل ملف البيانات التجريبية:

**الطريقة الأولى - استيراد:**
1. في phpMyAdmin، اذهب إلى **Import**
2. اختر `database/test_data.sql`
3. اضغط **Go**

**الطريقة الثانية - نسخ ولصق:**
1. افتح `database/test_data.sql`
2. انسخ المحتوى
3. في phpMyAdmin → **SQL** → الصق
4. اضغط **Go**

هذا الملف سينشئ:
- 👥 **5 طلاب تجريبيين** (student1 إلى student5)
- 📚 **3 دورات** (برمجة، تصميم، تسويق)
- 📝 **2 اختبار شامل** مع أسئلة متنوعة
- 📊 **درجات واقعية** لجميع الطلاب
- 🚨 **سجلات منع غش** تجريبية

### بيانات تسجيل الدخول التجريبية:

```
الطالب 1:
Username: student1
Password: (استخدم كلمة المرور الافتراضية)

الطالب 2:
Username: student2
Password: (استخدم كلمة المرور الافتراضية)
```

---

## 📖 دليل الاستخدام {#usage-guide}

### 🎓 للطلاب:

#### 1. عرض الدرجات (Real-time):
```html
<!-- ضع هذا الكود في لوحة الطالب -->
<iframe src="Manager/components/student_grades_widget.html" 
        width="100%" 
        height="800px" 
        frameborder="0"></iframe>
```

**الميزات:**
- ✅ تحديث تلقائي كل 10 ثواني
- ✅ عرض مكونات الدرجات بالتفصيل
- ✅ نسب مئوية ملونة حسب الأداء
- ✅ زر تحميل الشهادة للناجحين
- ✅ إشعارات فورية عند التحديث

#### 2. دخول الاختبار:
```
Manager/exam_interface.html?exam_id=1
```

**التعليمات:**
- ⚠️ سيتم فرض وضع ملء الشاشة
- ⚠️ لا يمكن التبديل إلى صفحات أخرى
- ⚠️ محاولات الغش ستُسجل تلقائياً
- ⚠️ سيتم الإرسال التلقائي بعد 3 تحذيرات

---

### 👨‍💼 للمشرفين (Manager/Technical):

#### 1. إدخال الدرجات:
```
افتح: Manager/grades_entry.html
```

**الخطوات:**
1. ✅ اختر الدورة من القائمة المنسدلة
2. ✅ عدّل مكونات التقييم (الاسم، الحد الأقصى، الوزن)
3. ✅ أدخل درجات الطلاب في الجدول
4. ✅ اضغط "حفظ جميع الدرجات"
5. ✅ سيتم إخطار الطلاب تلقائياً

**الميزات:**
- 🔄 حساب تلقائي للدرجة النهائية
- 🎨 ألوان ديناميكية (أخضر = نجاح، أحمر = رسوب)
- 📜 إصدار شهادات للناجحين بنقرة واحدة
- 📊 تصدير إلى Excel
- 🔔 إرسال إشعارات جماعية

---

### 👨‍🏫 للمدربين:

#### 1. إنشاء اختبار جديد:

```php
// استخدم API مباشرة
POST Manager/api/exams_system.php

{
  "action": "create",
  "course_id": 1,
  "title": "اختبار نهائي",
  "description": "اختبار شامل",
  "duration_minutes": 60,
  "total_marks": 50,
  "passing_percentage": 50,
  "questions": [
    {
      "type": "mcq",
      "text": "ما هي عاصمة اليمن؟",
      "options": ["عدن", "صنعاء", "تعز", "حضرموت"],
      "correct": "صنعاء",
      "marks": 5
    }
  ]
}
```

#### 2. نشر الاختبار:

```php
POST Manager/api/exams_system.php

{
  "action": "publish",
  "exam_id": 1
}
```

سيتم إرسال إشعار تلقائي لجميع الطلاب المسجلين.

---

## 🖥️ الواجهات المتاحة {#interfaces}

### 1️⃣ واجهة الطالب - الدرجات
**الملف:** `Manager/components/student_grades_widget.html`

**الوظائف:**
```javascript
class GradesDashboard {
  async loadGrades()           // تحميل الدرجات
  startRealTimeUpdates()       // بدء التحديث التلقائي
  renderGrades()               // عرض الدرجات
  detectChanges(newGrades)     // كشف التحديثات
  showNotification(message)    // إظهار الإشعار
}
```

**API المستخدمة:**
```
GET api/grades_system.php?action=get_grades
```

---

### 2️⃣ واجهة المشرفين - إدخال الدرجات
**الملف:** `Manager/grades_entry.html`

**الوظائف:**
```javascript
async loadCourses()              // تحميل الدورات
async loadStudents(courseId)    // تحميل طلاب الدورة
renderComponents()               // عرض مكونات التقييم
updateGrade(studentId, ...)     // تحديث درجة طالب
async saveAllGrades()           // حفظ جميع الدرجات
exportToExcel()                 // تصدير إلى Excel
```

**API المستخدمة:**
```
GET  api/get_enrolled_students.php?course_id=1
GET  api/grades_system.php?action=get_all_grades&course_id=1
POST api/grades_system.php (action: update_grades)
```

---

### 3️⃣ واجهة الاختبار - منع الغش
**الملف:** `Manager/exam_interface.html`

**نظام منع الغش:**
```javascript
class AntiCheatSystem {
  preventContextMenu()        // منع القائمة اليمنى
  preventCopyPaste()         // منع النسخ واللصق
  detectTabSwitch()          // كشف التبديل بين التبويبات
  detectFullscreenExit()     // فرض ملء الشاشة
  preventScreenCapture()     // منع التقاط الشاشة
  detectDevTools()           // كشف فتح أدوات المطور
  logEvent(type, data)       // تسجيل الحدث
  autoSubmitExam()           // إرسال تلقائي بعد تحذيرات
}
```

---

## 🚨 نظام منع الغش {#anti-cheat}

### الطرق المستخدمة:

| الطريقة | الوصف | الشدة | الإجراء |
|---------|-------|-------|---------|
| **Tab Switch** | كشف التبديل إلى صفحات أخرى | متوسطة | تحذير بعد 3 مرات → إرسال تلقائي |
| **Copy/Paste** | منع النسخ واللصق | عالية | تسجيل المحاولة |
| **Fullscreen Exit** | إجبار وضع ملء الشاشة | متوسطة | طلب إعادة الوضع |
| **Right Click** | منع القائمة اليمنى | منخفضة | منع الحدث |
| **DevTools** | كشف فتح أدوات المطور | عالية | تحذير فوري |
| **Screen Capture** | كشف PrintScreen | متوسطة | تسجيل المحاولة |

### مثال على سجل منع الغش:

```sql
SELECT * FROM exam_anti_cheat_log WHERE attempt_id = 1;
```

النتيجة:
```
| event_type       | severity | event_data                          |
|-----------------|----------|-------------------------------------|
| tab_switch      | medium   | {"count": 1, "timestamp": "..."}    |
| copy_attempt    | high     | {"question_id": 5}                  |
| visibility_change| medium   | {"count": 2, "duration": 15}        |
```

---

## 📜 التكامل مع الشهادات {#certificates}

### التوليد التلقائي:

عندما يتم إدخال درجات الطالب ويصل إلى **50% فأكثر**:

1. ✅ يتم تحديث حالة التسجيل → `completed`
2. ✅ يتم استدعاء API الشهادات تلقائياً
3. ✅ يتم إنشاء الشهادة مع صورة الطالب والشعار
4. ✅ يتم إرسال إشعار للطالب
5. ✅ يظهر زر "تحميل الشهادة" في لوحة الطالب

### الكود المسؤول:

```php
// في grades_system.php
function generateCertificate($conn, $student_id, $course_id, $issued_by) {
    $url = "http://localhost/Manager/api/generate_certificate.php";
    
    $data = [
        'student_id' => $student_id,
        'course_id' => $course_id,
        'issued_by' => $issued_by,
        'auto_generated' => 1
    ];
    
    // إنشاء الشهادة تلقائياً
    $ch = curl_init($url);
    curl_setopt($ch, CURLOPT_POST, 1);
    curl_setopt($ch, CURLOPT_POSTFIELDS, http_build_query($data));
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    $result = curl_exec($ch);
    curl_close($ch);
}
```

---

## 🔌 واجهات API

### 1. Exams System API

```
POST Manager/api/exams_system.php
```

**الإجراءات المتاحة:**

#### إنشاء اختبار (create):
```json
{
  "action": "create",
  "course_id": 1,
  "title": "اختبار نهائي",
  "duration_minutes": 90,
  "total_marks": 50,
  "passing_percentage": 50,
  "questions": [...]
}
```

#### نشر اختبار (publish):
```json
{
  "action": "publish",
  "exam_id": 1
}
```

#### بدء محاولة (start_attempt):
```json
{
  "action": "start_attempt",
  "exam_id": 1
}
```

#### إرسال إجابات (submit):
```json
{
  "action": "submit",
  "attempt_id": 1,
  "answers": [
    {
      "question_id": 1,
      "answer": "PHP"
    }
  ],
  "anti_cheat_events": [...]
}
```

---

### 2. Grades System API

```
POST Manager/api/grades_system.php
```

**الإجراءات المتاحة:**

#### تحديث درجات (update_grades):
```json
{
  "action": "update_grades",
  "student_id": 5,
  "course_id": 1,
  "grades": [
    {
      "component": "الحضور",
      "value": 10,
      "max": 10,
      "weight": 0.1
    }
  ]
}
```

#### عرض درجات الطالب (get_grades):
```
GET Manager/api/grades_system.php?action=get_grades
```

#### عرض جميع الدرجات (get_all_grades):
```
GET Manager/api/grades_system.php?action=get_all_grades&course_id=1
```

---

## 🧪 اختبار النظام

### 1. اختبار الدرجات:

```bash
# افتح واجهة الطالب
http://localhost/Manager/components/student_grades_widget.html

# افتح واجهة المشرفين
http://localhost/Manager/grades_entry.html

# أدخل درجات جديدة ولاحظ التحديث الفوري في واجهة الطالب
```

### 2. اختبار الاختبارات:

```bash
# افتح واجهة الاختبار
http://localhost/Manager/exam_interface.html?exam_id=1

# جرّب:
- التبديل بين التبويبات (سيظهر تحذير)
- محاولة النسخ (سيُمنع)
- الخروج من ملء الشاشة (سيُطلب العودة)
```

### 3. اختبار منع الغش:

```sql
-- بعد إكمال الاختبار، تحقق من السجلات:
SELECT * FROM exam_anti_cheat_log 
WHERE attempt_id = (SELECT id FROM exam_attempts ORDER BY id DESC LIMIT 1);
```

---

## 🎨 التخصيص

### تغيير الألوان:

```css
/* في exam_interface.html أو student_grades_widget.html */

/* الدرجة الممتازة */
.text-green-600 { color: #your-color; }

/* الدرجة الجيدة */
.text-blue-600 { color: #your-color; }

/* الدرجة الضعيفة */
.text-red-600 { color: #your-color; }
```

### تغيير الأوزان الافتراضية:

```javascript
// في grades_entry.html
let components = [
    { name: 'الحضور', max: 10, weight: 0.1 },      // 10%
    { name: 'المشاركة', max: 10, weight: 0.1 },    // 10%
    { name: 'الواجبات', max: 20, weight: 0.2 },    // 20%
    { name: 'الاختبار النصفي', max: 20, weight: 0.2 }, // 20%
    { name: 'الاختبار النهائي', max: 40, weight: 0.4 }  // 40%
];
```

### تغيير نسبة النجاح:

```php
// في grades_system.php
$passing_grade = 50.0; // غيّر هذه القيمة
```

---

## 📊 التقارير

### 1. تقرير الدرجات الكامل:

```sql
SELECT * FROM student_grades_summary
WHERE course_id = 1
ORDER BY final_grade DESC;
```

### 2. تقرير نتائج الاختبارات:

```sql
SELECT * FROM exam_results_summary
WHERE exam_id = 1
ORDER BY percentage DESC;
```

### 3. تقرير محاولات الغش:

```sql
SELECT 
    u.full_name,
    e.title AS exam_title,
    COUNT(acl.id) AS violations,
    GROUP_CONCAT(DISTINCT acl.event_type) AS violation_types
FROM exam_anti_cheat_log acl
INNER JOIN exam_attempts ea ON acl.attempt_id = ea.id
INNER JOIN users u ON ea.student_id = u.id
INNER JOIN exams e ON ea.exam_id = e.id
GROUP BY ea.student_id, ea.exam_id
HAVING violations > 0
ORDER BY violations DESC;
```

---

## ⚡ الأداء والتحسين

### 1. Indexes (تم إضافتها تلقائياً):

```sql
-- في exams_grades_schema.sql
CREATE INDEX idx_course_id ON exams(course_id);
CREATE INDEX idx_exam_id ON exam_questions(exam_id);
CREATE INDEX idx_attempt_id ON exam_answers(attempt_id);
CREATE INDEX idx_student_course ON student_grades(student_id, course_id);
```

### 2. Caching (اختياري):

```php
// إضافة caching للدرجات
$cache_file = "cache/grades_$student_id_$course_id.json";
if (file_exists($cache_file) && time() - filemtime($cache_file) < 10) {
    echo file_get_contents($cache_file);
    exit;
}
```

---

## 🛠️ استكشاف الأخطاء

### المشكلة: لا تظهر الدرجات

**الحل:**
```sql
-- تحقق من وجود درجات
SELECT * FROM student_grades WHERE student_id = YOUR_ID;

-- تحقق من التسجيل
SELECT * FROM enrollments WHERE student_id = YOUR_ID;
```

### المشكلة: لا يعمل منع الغش

**الحل:**
```javascript
// تحقق من دعم المتصفح
if (!document.fullscreenEnabled) {
    console.error('Fullscreen not supported');
}

// تحقق من Permissions
navigator.permissions.query({name: 'clipboard-write'});
```

### المشكلة: لا تُصدر الشهادات تلقائياً

**الحل:**
```php
// تحقق من تفعيل cURL
if (!function_exists('curl_init')) {
    die('cURL is not installed!');
}

// تحقق من الـ Trigger
SHOW TRIGGERS LIKE 'after_grade_insert_update';
```

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:

1. ✅ تحقق من ملف `error_log.txt`
2. ✅ راجع قسم "استكشاف الأخطاء"
3. ✅ تأكد من تثبيت جميع المتطلبات
4. ✅ تحقق من صلاحيات الملفات

---

## ✅ قائمة التحقق النهائية

قبل الإطلاق:

- [ ] قاعدة البيانات مثبتة بنجاح
- [ ] البيانات التجريبية تعمل
- [ ] واجهة الطالب تعرض الدرجات
- [ ] واجهة المشرفين تعمل
- [ ] الاختبارات تُنشأ وتُنشر بنجاح
- [ ] منع الغش يعمل بشكل صحيح
- [ ] الشهادات تُصدر تلقائياً
- [ ] الإشعارات تُرسل للطلاب
- [ ] GD Extension مفعّلة
- [ ] جميع الصلاحيات صحيحة

---

## 🎉 تهانينا!

النظام جاهز للاستخدام! 🚀

**الملفات الرئيسية:**
- ✅ `Manager/api/exams_system.php` - API الاختبارات
- ✅ `Manager/api/grades_system.php` - API الدرجات
- ✅ `Manager/exam_interface.html` - واجهة الاختبار
- ✅ `Manager/components/student_grades_widget.html` - واجهة الطالب
- ✅ `Manager/grades_entry.html` - واجهة المشرفين
- ✅ `database/exams_grades_schema.sql` - هيكل قاعدة البيانات
- ✅ `database/test_data.sql` - بيانات تجريبية

---

**تم التطوير بواسطة:** نظام إبداع تعز  
**التاريخ:** 2024  
**الإصدار:** 1.0.0
