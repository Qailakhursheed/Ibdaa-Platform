# دليل الاستخدام السريع - نظام التسجيل الموحد
## Quick Start Guide - Unified Registration System

---

## 🚀 البدء السريع

### 1. للطلاب - تقديم طلب انضمام

**الرابط:**
```
http://localhost/Ibdaa-Taiz/platform/unified_registration.php
```

**الخطوات:**
1. افتح رابط التسجيل أعلاه
2. **الخطوة 1 - البيانات الشخصية:**
   - الاسم الكامل (ثلاثي أو رباعي)
   - البريد الإلكتروني
   - رقم الهاتف (00967xxxxxxxxx)
   - تاريخ الميلاد (15 سنة على الأقل)
   - المحافظة والمديرية
3. اضغط "التالي"
4. **الخطوة 2 - اختيار الدورة:**
   - اختر دورة من القائمة
5. اضغط "التالي"
6. **الخطوة 3 - المستندات:**
   - ارفع صورة الهوية (JPG/PNG/PDF - Max 5MB)
   - ارفع صورة شخصية (JPG/PNG - Max 5MB)
   - أضف ملاحظات (اختياري)
7. اضغط "إرسال الطلب"
8. **احفظ رقم الطلب** الذي يظهر لك (مثال: #12345)

**النتيجة:**
- ✅ تم إرسال طلبك بنجاح
- ✅ ستصلك رسالة تأكيد على البريد الإلكتروني
- ⏳ انتظر 24-48 ساعة للمراجعة

---

### 2. للمشرف الفني - مراجعة الطلبات

**تسجيل الدخول:**
```
Email: tech_supervisor@ibdaa.local
Password: Test@123
```

**المهام:**
1. مراجعة الطلبات المعلقة
2. التحقق من بيانات الطالب والمستندات
3. قبول أو رفض الطلب مع السبب

**استعلام للطلبات المعلقة:**
```sql
SELECT * FROM pending_applications;
```

**عند القبول:**
```sql
UPDATE applications 
SET 
    status = 'approved',
    reviewed_by = YOUR_USER_ID,
    reviewed_at = NOW(),
    review_notes = 'ملاحظات المشرف'
WHERE id = APPLICATION_ID;
```

**عند الرفض:**
```sql
UPDATE applications 
SET 
    status = 'rejected',
    reviewed_by = YOUR_USER_ID,
    reviewed_at = NOW(),
    review_notes = 'سبب الرفض'
WHERE id = APPLICATION_ID;
```

---

### 3. للمدير المالي - تأكيد الدفع

**تسجيل الدخول:**
```
Email: admin_manager@ibdaa.local
Password: Test@123
```

**المهام:**
1. مراجعة الطلبات المقبولة بانتظار الدفع
2. استلام الدفع من الطالب (نقداً أو تحويل)
3. تأكيد الدفع في النظام
4. **النظام سيقوم تلقائياً بـ:**
   - إنشاء حساب للطالب
   - تفعيل الحساب
   - إنشاء سجل انضمام
   - إرسال بيانات الدخول بالبريد

**استعلام للطلبات بانتظار الدفع:**
```sql
SELECT * FROM approved_pending_payment;
```

**تأكيد الدفع:**
```sql
-- خطوة واحدة: تحديث payment_status
UPDATE applications 
SET payment_status = 'completed'
WHERE id = APPLICATION_ID;

-- سيقوم Trigger بالباقي تلقائياً!
```

---

### 4. للطالب - تسجيل الدخول بعد التفعيل

**الرابط:**
```
http://localhost/Ibdaa-Taiz/platform/login.php
```

**البيانات:**
- سيتم إرسال اسم المستخدم وكلمة المرور على البريد الإلكتروني بعد تأكيد الدفع

**بعد الدخول:**
- ✅ الوصول إلى لوحة التحكم
- ✅ مشاهدة الدورات المسجل بها
- ✅ حضور المحاضرات
- ✅ تقديم الامتحانات

---

## 🔍 الاستعلامات المفيدة

### 1. عرض جميع الطلبات
```sql
SELECT 
    id,
    full_name,
    email,
    course_name,
    status,
    payment_status,
    created_at
FROM applications
ORDER BY created_at DESC;
```

### 2. عرض الطلبات المعلقة فقط
```sql
SELECT * FROM pending_applications;
```

### 3. عرض الطلبات المقبولة بانتظار الدفع
```sql
SELECT * FROM approved_pending_payment;
```

### 4. عرض الطلاب النشطين
```sql
SELECT * FROM active_enrollments;
```

### 5. البحث عن طلب معين
```sql
SELECT * FROM applications WHERE id = 12345;
-- أو
SELECT * FROM applications WHERE email = 'student@example.com';
```

---

## 🔧 الصيانة والإدارة

### 1. حذف طلب
```sql
DELETE FROM applications WHERE id = APPLICATION_ID;
```

### 2. تحديث بيانات طلب
```sql
UPDATE applications 
SET 
    phone = 'new_phone',
    governorate = 'new_governorate'
WHERE id = APPLICATION_ID;
```

### 3. إعادة تعيين حالة طلب
```sql
UPDATE applications 
SET 
    status = 'pending',
    payment_status = 'pending',
    reviewed_by = NULL,
    reviewed_at = NULL
WHERE id = APPLICATION_ID;
```

### 4. عرض إحصائيات
```sql
SELECT 
    status,
    COUNT(*) AS total,
    COUNT(*) * 100.0 / (SELECT COUNT(*) FROM applications) AS percentage
FROM applications
GROUP BY status;
```

---

## 📊 الحسابات التجريبية

### المدير
```
Username: admin_manager
Email: admin_manager@ibdaa.local
Password: Test@123
Role: manager
```

### المشرف الفني
```
Username: tech_supervisor
Email: tech_supervisor@ibdaa.local
Password: Test@123
Role: technical
```

### المدرب
```
Username: trainer_ahmad
Email: trainer_ahmad@ibdaa.local
Password: Test@123
Role: trainer
```

### الطالب التجريبي
```
Username: student_mohammed
Email: student_mohammed@ibdaa.local
Password: Test@123
Role: student
```

---

## ⚠️ نصائح مهمة

### 1. للأمان
- 🔒 غيّر كلمات المرور الافتراضية فوراً
- 🔒 لا تشارك بيانات الدخول
- 🔒 استخدم HTTPS في الإنتاج

### 2. للأداء
- ⚡ نظف جدول `login_attempts` دورياً
- ⚡ أرشف الطلبات القديمة (أكثر من سنة)
- ⚡ راقب حجم مجلد uploads

### 3. للبيانات
- 💾 انسخ احتياطياً يومياً
- 💾 احفظ نسخة من قاعدة البيانات قبل أي تحديث
- 💾 اختبر الاستعادة شهرياً

---

## 🆘 حل المشاكل الشائعة

### مشكلة 1: لا يمكن رفع الملفات
```bash
# الحل: تحقق من صلاحيات المجلد
chmod 755 platform/uploads/applications/
```

### مشكلة 2: خطأ في قاعدة البيانات
```sql
-- الحل: أعد إنشاء الجدول
SOURCE database/UNIFIED_REGISTRATION_SYSTEM.sql;
```

### مشكلة 3: تم تجاوز الحد المسموح من المحاولات
```sql
-- الحل: احذف السجل من login_attempts
DELETE FROM login_attempts 
WHERE identifier LIKE 'registration_%';
```

### مشكلة 4: الإشعارات لا ترسل
```sql
-- التحقق من الإشعارات المعلقة
SELECT * FROM notification_log WHERE status = 'pending';

-- ملاحظة: نظام البريد الفعلي لم يُنشأ بعد
-- الإشعارات محفوظة في قاعدة البيانات فقط حالياً
```

---

## 📞 للدعم

**الملفات المرجعية:**
1. `UNIFIED_REGISTRATION_COMPLETION_REPORT.md` - التقرير الشامل
2. `SECURITY_IMPROVEMENTS_REPORT.md` - تقرير الأمان
3. `ANTI_DETECTION_REPORT.md` - تقرير مكافحة الكشف

**الملفات الرئيسية:**
- `platform/unified_registration.php` - صفحة التسجيل
- `platform/process_registration.php` - معالج التسجيل
- `database/UNIFIED_REGISTRATION_SYSTEM.sql` - SQL الشامل

---

**آخر تحديث:** 12 نوفمبر 2025  
**النسخة:** 1.0.0  
**الحالة:** ✅ جاهز للاستخدام
