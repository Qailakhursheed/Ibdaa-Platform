# 🎴 نظام بطاقات الطلاب - Student ID Card System
## التوثيق الكامل | Phase 5 من مشروع إبداع

---

## 📋 **نظرة عامة**

نظام احترافي لإنشاء وطباعة وإرسال بطاقات الطلاب بصيغة PDF مع رمز QR للتحقق.

### المكونات الرئيسية:
1. **generate_id_card.php** - توليد بطاقة PDF
2. **verify_student.php** - صفحة التحقق من البطاقة
3. **send_card_to_student.php** - إرسال البطاقة بالبريد
4. **dashboard.php** - أزرار التحكم (طباعة + إرسال)

---

## 🏗️ **البنية التقنية**

### 1️⃣ **generate_id_card.php**
📁 المسار: `Manager/api/generate_id_card.php`

#### الوظيفة:
- توليد بطاقة طالب بصيغة PDF بحجم بطاقة ائتمان (85.6mm × 53.98mm)
- إضافة رمز QR Code يؤدي لصفحة التحقق
- تصميم احترافي بشعار المنصة (watermark + logo صغير)

#### المدخلات:
```php
GET: ?id=123  // رقم الطالب
```

#### الحماية:
```php
// يسمح بالوصول:
// 1. المديرون (manager)
// 2. المشرفون الفنيون (technical)
// 3. الطالب نفسه (user_id == student_id)
$allowed = in_array($user_role, ['manager', 'technical']) || ($user_id == $student_id);
```

#### المكتبات:
```php
use setasign\Fpdf\Fpdf;
use chillerlan\QRCode\QRCode;
use chillerlan\QRCode\QROptions;
```

#### المخرجات:
- PDF يفتح في نافذة جديدة (Output mode: 'I')
- اسم الملف: `student_card_123.pdf`

#### عناصر التصميم:
| العنصر | التفاصيل |
|--------|----------|
| **الحجم** | 85.6mm × 53.98mm (حجم بطاقة ائتمان قياسي) |
| **الخلفية** | لون أزرق فاتح (RGB: 240, 248, 255) |
| **الإطار** | خط أزرق بسماكة 0.5mm |
| **الشعار** | watermark شفاف (10% opacity) + شعار صغير أعلى اليسار |
| **العنوان** | "بطاقة طالب" بالعربية + "Student ID Card" بالإنجليزية |
| **البيانات** | اسم الطالب، رقمه، الدورة، الموقع، تاريخ التسجيل |
| **رمز QR** | 18mm × 18mm في أسفل اليسار + نص "Scan to Verify" |
| **الفوتر** | "منصة إبداع للتدريب والتأهيل - Ibdaa Training Platform" |

#### مسار الشعار:
```php
$logoPath = __DIR__ . '/../../platform/photos/Sh.jpg';
```
⚠️ تأكد من وجود الملف في هذا المسار.

#### مثال رابط التحقق:
```
http://localhost/Ibdaa-Taiz/platform/verify_student.php?id=123
```

---

### 2️⃣ **verify_student.php**
📁 المسار: `platform/verify_student.php`

#### الوظيفة:
صفحة عامة (public) تتحقق من صحة بطاقة الطالب عند مسح رمز QR.

#### المدخلات:
```
GET: ?id=123
```

#### السيناريوهات:

**✅ طالب موجود:**
- عرض رمز ✅ كبير
- عرض بيانات الطالب:
  * الاسم الكامل
  * رقم الطالب (#000123)
  * الدورة (إن وجدت)
  * البريد الإلكتروني
  * الموقع (المديرية - المحافظة)
  * تاريخ التسجيل
- رسالة تأكيد: "هذا الطالب عضو مسجل ومتحقق منه"

**❌ طالب غير موجود:**
- عرض رمز ❌ كبير
- رسالة: "لم يتم العثور على طالب بهذا الرقم"
- توجيه: "يرجى التحقق من صحة رمز QR"

#### التصميم:
- **Framework:** Tailwind CSS (عبر CDN)
- **الخط:** Tajawal (خط عربي احترافي)
- **الثيمة:** Gradient أزرق-بنفسجي (from-blue-50 to-indigo-100)
- **البطاقة:** مركزية، shadow كبير، rounded corners
- **الأيقونات:** SVG icons مضمنة

#### الاستعلام SQL:
```sql
SELECT u.id, u.full_name, u.email, u.governorate, u.district, u.created_at, 
       c.title as course_title
FROM users u 
LEFT JOIN enrollments e ON u.id = e.user_id 
LEFT JOIN courses c ON e.course_id = c.id 
WHERE u.id = ? AND u.role = 'student' 
LIMIT 1
```

#### الحماية:
- لا تتطلب تسجيل دخول (صفحة عامة)
- تعتمد على `intval()` لتنظيف رقم الطالب
- تستخدم `htmlspecialchars()` لحماية XSS

---

### 3️⃣ **send_card_to_student.php**
📁 المسار: `Manager/api/send_card_to_student.php`

#### الوظيفة:
توليد بطاقة PDF وإرسالها كـ attachment عبر البريد الإلكتروني.

#### الصلاحيات:
```php
if (!in_array($user_role, ['manager', 'technical'])) {
    exit(json_encode(['success' => false, 'message' => 'غير مصرح']));
}
```
⚠️ المشرفون الفنيون (technical) يمتلكون صلاحية الإرسال.

#### المدخلات:
```javascript
POST: student_id=123
```

#### خطوات التنفيذ:
1. **جلب بيانات الطالب** من قاعدة البيانات
2. **توليد رمز QR** (PNG مؤقت في uploads/temp/)
3. **إنشاء PDF** باستخدام FPDF (نفس تصميم generate_id_card.php)
4. **حفظ PDF مؤقتاً** في uploads/temp/
5. **إرسال Email** عبر PHPMailer مع PDF كـ attachment
6. **حذف الملفات المؤقتة** (QR + PDF)

#### بنية البريد:
```html
Subject: بطاقة الطالب - منصة إبداع

Body (HTML):
<div dir='rtl'>
    <h2>عزيزنا/عزيزتنا [اسم الطالب]</h2>
    <p>نرفق لك بطاقة الطالب الخاصة بك من منصة إبداع للتدريب والتأهيل.</p>
    <ul>
        <li>رقم الطالب: #000123</li>
        <li>الدورة: [اسم الدورة]</li>
    </ul>
    <p>يمكنك استخدام رمز QR للتحقق من صحة البطاقة.</p>
    <hr>
    <p style="color: gray;">منصة إبداع للتدريب والتأهيل</p>
</div>
```

#### الاستجابة JSON:
```json
{
    "success": true,
    "message": "تم إرسال البطاقة بنجاح إلى student@example.com"
}
```
أو:
```json
{
    "success": false,
    "message": "فشل إرسال البريد الإلكتروني"
}
```

#### المكتبات:
```php
require_once __DIR__ . '/../../Mailer/sendMail.php';
```
⚠️ تأكد من إعداد `sendMail.php` وPHPMailer بشكل صحيح.

#### الملفات المؤقتة:
```php
$qrTempPath = __DIR__ . '/../../uploads/temp/qr_' . $student_id . '_' . time() . '.png';
$pdfPath = __DIR__ . '/../../uploads/temp/card_' . $student_id . '_' . time() . '.pdf';
```

---

### 4️⃣ **تكامل لوحة التحكم**
📁 الملف: `Manager/dashboard.php`

#### الأزرار المضافة:

**1. زر طباعة البطاقة:**
```html
<button onclick="window.open('api/generate_id_card.php?id=${trainee.id}', '_blank')" 
        class="p-2 text-gray-400 hover:text-indigo-500" 
        title="طباعة بطاقة">
    <i data-lucide="credit-card" class="w-4 h-4"></i>
</button>
```
- **الأيقونة:** credit-card (بطاقة)
- **اللون:** indigo-500 عند hover
- **السلوك:** يفتح PDF في نافذة جديدة

**2. زر إرسال البطاقة:**
```html
<button onclick="sendCardToStudent(${trainee.id})" 
        class="p-2 text-gray-400 hover:text-purple-500" 
        title="إرسال بطاقة للطالب">
    <i data-lucide="mail" class="w-4 h-4"></i>
</button>
```
- **الأيقونة:** mail (بريد)
- **اللون:** purple-500 عند hover
- **السلوك:** يرسل POST لـ send_card_to_student.php

#### الدالة JavaScript:
```javascript
window.sendCardToStudent = async (studentId) => {
    if (!confirm('هل تريد إرسال البطاقة إلى البريد الإلكتروني للطالب؟')) return;
    
    const btn = event.target.closest('button');
    const originalHTML = btn.innerHTML;
    btn.disabled = true;
    btn.innerHTML = '<i data-lucide="loader" class="w-4 h-4 animate-spin"></i>';
    lucide.createIcons();
    
    try {
        const res = await fetch('api/send_card_to_student.php', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: `student_id=${studentId}`
        });
        const data = await res.json();
        
        if (data.success) {
            alert('✅ ' + data.message);
        } else {
            alert('❌ ' + (data.message || 'فشل الإرسال'));
        }
    } catch (err) {
        console.error(err);
        alert('❌ خطأ في الاتصال بالخادم');
    } finally {
        btn.disabled = false;
        btn.innerHTML = originalHTML;
        lucide.createIcons();
    }
};
```

#### ميزات UX:
- **تأكيد قبل الإرسال:** confirm dialog
- **مؤشر تحميل:** أيقونة spinner أثناء الإرسال
- **تعطيل الزر:** يمنع النقر المتعدد
- **استعادة الزر:** يعود لحالته الأصلية بعد الانتهاء
- **رسائل واضحة:** ✅ نجاح أو ❌ فشل

---

## 🔧 **متطلبات التشغيل**

### المكتبات المطلوبة:
```json
{
    "setasign/fpdf": "^2.6",
    "chillerlan/php-qrcode": "^4.3"
}
```
✅ تأكد من تثبيتها عبر Composer.

### ملفات النظام:
```
Manager/api/
├── generate_id_card.php       ✅
└── send_card_to_student.php   ✅

platform/
├── verify_student.php         ✅
└── photos/
    └── Sh.jpg                 ⚠️ (يجب أن يكون موجوداً)

uploads/temp/                  ✅ (مجلد مؤقت)
```

### الصلاحيات:
- **المدير (manager):** كامل الصلاحيات
- **المشرف الفني (technical):** طباعة + إرسال بطاقات الطلاب
- **الطلاب:** طباعة بطاقتهم فقط (عبر رابط مباشر)

### إعدادات PHPMailer:
تأكد من إعداد `Mailer/sendMail.php` مع:
- SMTP Server
- Username & Password
- From Email

---

## 🧪 **طريقة الاختبار**

### 1️⃣ **اختبار طباعة البطاقة:**
```
1. افتح المتصفح: http://localhost/Ibdaa-Taiz/Manager/dashboard.php
2. انتقل لقسم "الطلاب (المتدربين)"
3. اضغط على أيقونة credit-card 🎴
4. يجب أن يفتح PDF في نافذة جديدة
```
✅ **النتيجة المتوقعة:** بطاقة PDF بحجم بطاقة ائتمان + QR code

### 2️⃣ **اختبار التحقق من QR:**
```
1. استخدم تطبيق QR scanner على الهاتف
2. امسح رمز QR من البطاقة
3. يجب أن تفتح صفحة verify_student.php
```
✅ **النتيجة المتوقعة:** صفحة تعرض ✅ + بيانات الطالب

### 3️⃣ **اختبار إرسال البطاقة:**
```
1. اضغط على أيقونة mail 📧
2. أكد الإرسال في نافذة confirm
3. انتظر رسالة النجاح/الفشل
4. تحقق من بريد الطالب
```
✅ **النتيجة المتوقعة:** رسالة "✅ تم إرسال البطاقة بنجاح" + البطاقة في البريد

### 4️⃣ **اختبار التحقق اليدوي:**
```
1. افتح: http://localhost/Ibdaa-Taiz/platform/verify_student.php?id=1
2. يجب عرض بيانات الطالب رقم 1
3. افتح: verify_student.php?id=99999
4. يجب عرض رسالة ❌ "لم يتم العثور على طالب"
```

---

## 🔒 **الحماية والأمان**

### 1. **SQL Injection:**
```php
$stmt = $conn->prepare("SELECT ... WHERE u.id = ? AND u.role = 'student'");
$stmt->bind_param('i', $student_id);
```
✅ استخدام Prepared Statements

### 2. **XSS Protection:**
```php
<?= htmlspecialchars($student['full_name']) ?>
```
✅ تطهير جميع المخرجات

### 3. **Access Control:**
```php
$allowed = in_array($user_role, ['manager', 'technical']) || ($user_id == $student_id);
if (!$allowed) die('Not authorized');
```
✅ فحص الصلاحيات قبل كل عملية

### 4. **تنظيف المدخلات:**
```php
$student_id = intval($_GET['id'] ?? 0);
```
✅ تحويل لـ integer يمنع SQL injection

### 5. **حذف الملفات المؤقتة:**
```php
if (file_exists($qrTempPath)) unlink($qrTempPath);
if (file_exists($pdfPath)) unlink($pdfPath);
```
✅ منع تراكم الملفات على السيرفر

---

## 🐛 **استكشاف الأخطاء**

### ❌ **خطأ: "Undefined type 'setasign\Fpdf\Fpdf'"**
**السبب:** مكتبة FPDF غير مثبتة.
**الحل:**
```bash
composer require setasign/fpdf
```

### ❌ **خطأ: "QR code not appearing"**
**السبب:** مجلد uploads/temp غير موجود.
**الحل:**
```bash
mkdir uploads/temp
chmod 777 uploads/temp  # Linux
```

### ❌ **خطأ: "Logo not showing"**
**السبب:** ملف `platform/photos/Sh.jpg` مفقود.
**الحل:**
- رفع شعار المنصة إلى `platform/photos/Sh.jpg`
- أو تعديل المسار في الكود

### ❌ **خطأ: "Email not sent"**
**السبب:** إعدادات PHPMailer غير صحيحة.
**الحل:**
1. افتح `Mailer/sendMail.php`
2. تأكد من:
   - SMTP Host
   - SMTP Username
   - SMTP Password
   - From Email

### ❌ **خطأ: "verify_student.php shows white page"**
**السبب:** خطأ في اتصال قاعدة البيانات.
**الحل:**
- تحقق من `platform/db.php`
- تأكد من صحة بيانات الاتصال

---

## 📊 **الإحصائيات**

| الملف | عدد الأسطر | الحجم | الوظيفة |
|------|-----------|------|---------|
| generate_id_card.php | 180 | ~6 KB | توليد PDF |
| verify_student.php | 120 | ~5 KB | التحقق |
| send_card_to_student.php | 170 | ~7 KB | الإرسال |
| dashboard.php (إضافات) | 35 | ~1 KB | UI buttons |
| **الإجمالي** | **505** | **~19 KB** | - |

---

## 🎯 **الخلاصة**

✅ **تم تنفيذ Phase 5 بالكامل:**
1. ✅ مراجعة صلاحيات CRUD للمشرف الفني (technical)
2. ✅ بناء نظام بطاقات PDF احترافي + QR code
3. ✅ صفحة تحقق عامة (verify_student.php)
4. ✅ أزرار طباعة وإرسال في لوحة التحكم
5. ✅ وظيفة إرسال البطاقة بالبريد

**المكونات الجاهزة:**
- ✅ generate_id_card.php (توليد PDF)
- ✅ verify_student.php (التحقق)
- ✅ send_card_to_student.php (الإرسال)
- ✅ sendCardToStudent() (JavaScript function)
- ✅ أزرار UI في renderTrainees()

**الصلاحيات:**
- ✅ manager: كامل الصلاحيات
- ✅ technical: طباعة + إرسال بطاقات الطلاب
- ✅ student: طباعة بطاقته فقط

---

## 🚀 **الخطوة التالية**

### Phase 6: التقرير النهائي
```
📝 إنشاء تقرير شامل يوضح:
1. جميع الـ APIs المبنية (مع أمثلة طلبات)
2. كل ميزة في dashboard.php
3. المخططات (diagrams) للأنظمة
4. تعليمات النشر (deployment)
5. صيانة مستقبلية (maintenance guide)
```

---

**🎉 إبداع تايز - نظام متكامل من الصفر!**
