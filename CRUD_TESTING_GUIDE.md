# ⚡ دليل سريع: اختبار أزرار الإدارة (CRUD)

## 🚀 الخطوة 1: الوصول للوحة التحكم

افتح المتصفح واذهب إلى:
```
http://localhost/Ibdaa-Taiz/Manager/login.php
```

سجل دخول بحساب **مدير** أو **دعم فني**:
- البريد: `manager@ibdaa.com`
- كلمة المرور: `password123`

---

## 🧪 الاختبار 1: حذف متدرب

### الخطوات:
1. من القائمة الجانبية، اضغط على **"إدارة المتدربين"**
2. ستظهر جدول بجميع المتدربين
3. اختر أي متدرب واضغط على أيقونة 🗑️ (سلة المهملات)
4. ستظهر رسالة تأكيد: **"هل أنت متأكد من حذف المتدرب؟"**
5. اضغط **"OK"**

### النتيجة المتوقعة:
- ✅ رسالة: "تم الحذف بنجاح"
- ✅ إعادة تحميل الصفحة
- ✅ اختفاء المتدرب من الجدول

### في حالة الفشل:
- افتح Console (F12) وابحث عن أخطاء
- تحقق من أن API يعمل: `http://localhost/Ibdaa-Taiz/Manager/api/manage_users.php`

---

## 🧪 الاختبار 2: حذف مدرب

### الخطوات:
1. من القائمة الجانبية، اضغط على **"إدارة المدربين"**
2. ستظهر بطاقات المدربين (Cards)
3. في الزاوية العلوية اليسرى من أي بطاقة، اضغط على أيقونة 🗑️
4. تأكيد الحذف

### النتيجة المتوقعة:
- ✅ رسالة: "تم الحذف بنجاح"
- ✅ إعادة تحميل الصفحة
- ✅ اختفاء بطاقة المدرب

---

## 🧪 الاختبار 3: حذف دورة

### الخطوات:
1. من القائمة الجانبية، اضغط على **"البرامج والدورات"**
2. ستظهر بطاقات الدورات
3. على الجانب الأيسر من أي بطاقة، اضغط على أيقونة 🗑️
4. تأكيد الحذف

### النتيجة المتوقعة (حالة النجاح):
- ✅ رسالة: "تم حذف الدورة بنجاح"
- ✅ اختفاء بطاقة الدورة

### النتيجة المتوقعة (حالة الفشل):
- ⚠️ رسالة: "لا يمكن حذف الدورة لوجود طلاب مسجلين فيها"
- ℹ️ هذا يحدث إذا كانت الدورة لها طلاب مسجلين

---

## 🧪 الاختبار 4: أزرار الإضافة

### اختبار زر "إضافة متدرب جديد":
1. اذهب إلى **"إدارة المتدربين"**
2. اضغط على الزر الأزرق **"إضافة متدرب جديد"**

**النتيجة الحالية**: 
- سيفتح Modal موجود مسبقاً (من التحديثات السابقة)

---

### اختبار زر "إضافة مدرب جديد":
1. اذهب إلى **"إدارة المدربين"**
2. اضغط على الزر الأزرق **"إضافة مدرب جديد"**

**النتيجة الحالية**: 
- ⚠️ رسالة: "سيتم فتح نموذج إضافة مدرب جديد قريباً"
- ℹ️ Modal لم يتم تنفيذه بعد (TODO)

---

### اختبار زر "إضافة دورة جديدة":
1. اذهب إلى **"البرامج والدورات"**
2. اضغط على الزر الأزرق **"إضافة دورة جديدة"**

**النتيجة الحالية**: 
- ⚠️ رسالة: "سيتم فتح نموذج إضافة دورة جديدة قريباً"
- ℹ️ Modal لم يتم تنفيذه بعد (TODO)

---

## 🧪 الاختبار 5: أزرار التعديل

### اختبار تعديل متدرب:
1. اذهب إلى **"إدارة المتدربين"**
2. اضغط على أيقونة ✏️ (تعديل) بجانب أي متدرب

**النتيجة الحالية**: 
- ⚠️ رسالة: "سيتم فتح نموذج تعديل بيانات المستخدم #X قريباً"
- ℹ️ Modal لم يتم تنفيذه بعد (TODO)

---

### اختبار تعديل مدرب:
1. اذهب إلى **"إدارة المدربين"**
2. اضغط على أيقونة ✏️ في بطاقة أي مدرب

**النتيجة الحالية**: 
- ⚠️ رسالة: "سيتم فتح نموذج تعديل بيانات المستخدم #X قريباً"

---

### اختبار تعديل دورة:
1. اذهب إلى **"البرامج والدورات"**
2. اضغط على أيقونة ✏️ في بطاقة أي دورة

**النتيجة الحالية**: 
- ⚠️ رسالة: "سيتم فتح نموذج تعديل الدورة #X قريباً"

---

## 🔍 فحص Console للأخطاء

افتح أدوات المطور (F12) → تبويب **Console**

### اختبار API مباشرة:

#### 1. جلب المتدربين:
افتح في متصفح جديد:
```
http://localhost/Ibdaa-Taiz/Manager/api/manage_users.php?role=student
```

يجب أن تظهر:
```json
{
  "success": true,
  "data": [...],
  "count": 245
}
```

#### 2. جلب المدربين:
```
http://localhost/Ibdaa-Taiz/Manager/api/manage_users.php?role=trainer
```

#### 3. جلب الدورات:
```
http://localhost/Ibdaa-Taiz/Manager/api/manage_courses.php?status=all
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "غير مصرح لك"
**السبب**: تسجيل الدخول بحساب طالب أو مدرب
**الحل**: سجل دخول بحساب manager أو technical

### المشكلة 2: أزرار الحذف لا تعمل
**السبب**: JavaScript لم يتم تحميله
**الحل**: 
- افتح Console (F12)
- تحقق من وجود أخطاء JavaScript
- تحقق من أن Lucide icons يتم تحميله

### المشكلة 3: "لا يمكن حذف الدورة"
**السبب**: الدورة لها طلاب مسجلين
**الحل**: 
- هذا سلوك متوقع (حماية من الحذف العرضي)
- لحذف الدورة، يجب أولاً إلغاء تسجيل جميع الطلاب

### المشكلة 4: API يعيد خطأ 500
**الحل**:
- تحقق من أن XAMPP يعمل (Apache + MySQL)
- تحقق من أن قاعدة البيانات `ibdaa_platform` موجودة
- تحقق من ملفات `api/manage_users.php` و `api/manage_courses.php`

---

## 📊 ملخص الحالة

### ✅ يعمل الآن:
- حذف متدرب
- حذف مدرب
- حذف دورة
- زر "إضافة متدرب جديد" (Modal موجود)

### ⏳ قريباً (TODO):
- Modal لإضافة مدرب
- Modal لإضافة دورة
- Modal لتعديل المتدرب
- Modal لتعديل المدرب
- Modal لتعديل الدورة

---

## 📞 الدعم الفني

إذا واجهت مشكلة:
1. افتح Console (F12) وابحث عن أخطاء
2. افتح Network Tab (F12) → تحقق من استجابات API
3. تحقق من ملف التقرير الشامل: `CRUD_ACTIVATION_REPORT.md`

---

© 2024 منصة إبداع للتدريب
