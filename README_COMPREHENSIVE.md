# 🎓 منصة إبداع للتدريب والتأهيل

**نظام متكامل لإدارة الدورات التدريبية والطلاب والمدربين**

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![PHP](https://img.shields.io/badge/PHP-7.4%2B-purple.svg)
![Status](https://img.shields.io/badge/status-Production%20Ready-brightgreen.svg)

---

## 📋 نظرة عامة على المشروع

منصة إبداع هي نظام تدريب متكامل يوفر:

- ✅ **لوحة تحكم المدير** - إدارة الطلبات والدورات والمستخدمين
- ✅ **لوحة تحكم المتابع الفني** - متابعة الطلبات والإجراءات الإدارية
- ✅ **لوحة تحكم المدرب** - إدارة الدورات والواجبات والاختبارات والدرجات
- ✅ **لوحة تحكم الطالب** - التسجيل والتحضير والواجبات والعلامات
- ✅ **نظام دردشة متكامل** - تواصل فعال بين جميع المستخدمين
- ✅ **نظام الجداول الزمنية** - جدولة الدروس والمحاضرات
- ✅ **نظام الحضور والغياب** - تتبع تفاعل الطلاب
- ✅ **نظام التقارير والإحصائيات** - رسومات بيانية تفاعلية
- ✅ **نظام الشهادات** - إصدار شهادات إكمال الدورات

---

## 🏗️ هيكل المشروع

```
Ibdaa-Taiz/
├── 📁 platform/                      # الموقع الرئيسي
│   ├── index.php                    # الصفحة الرئيسية
│   ├── signup.php                   # تسجيل مستخدم جديد
│   ├── login.php                    # صفحة تسجيل الدخول
│   ├── dashboard/                   # لوحات التحكم
│   │   ├── admin/                   # لوحة المدير
│   │   ├── technical/               # لوحة المتابع الفني
│   │   ├── trainer/                 # لوحة المدرب
│   │   └── student/                 # لوحة الطالب
│   ├── courses/                     # إدارة الدورات
│   ├── chat/                        # نظام الدردشة
│   ├── assignments/                 # الواجبات والاختبارات
│   ├── grades/                      # نظام الدرجات
│   ├── attendance/                  # الحضور والغياب
│   ├── certificates/                # الشهادات
│   ├── api/                         # REST API
│   ├── uploads/                     # الملفات المرفوعة
│   │   ├── ids/                     # بطاقات الهوية
│   │   ├── photos/                  # صور المستخدمين
│   │   ├── documents/               # الوثائق
│   │   └── certificates/            # ملفات الشهادات
│   ├── css/                         # ملفات CSS
│   ├── js/                          # ملفات JavaScript
│   └── db.php                       # اتصال قاعدة البيانات
│
├── 📁 api-v2/                       # REST API v2
│   ├── endpoints/                   # نقاط النهاية
│   ├── middleware/                  # المسقطات
│   ├── models/                      # نماذج البيانات
│   └── config.php                   # إعدادات API
│
├── 📁 Mailer/                       # نظام البريد الإلكتروني
│   ├── sendMail.php                 # إرسال الإشعارات
│   └── templates/                   # قوالب البريد
│
├── 📁 Manager/                      # لوحة المدير
│   ├── index.html
│   ├── requests.php
│   └── dashboard.php
│
├── 📁 database/                     # قاعدة البيانات
│   ├── schema.sql                   # هيكل قاعدة البيانات
│   ├── initial_setup.sql            # البيانات الأولية
│   ├── migrations/                  # تحديثات قاعدة البيانات
│   └── sample_data.sql              # بيانات نموذجية
│
├── 📁 docs/                         # التوثيق
│   ├── API.md                       # توثيق API
│   ├── SETUP.md                     # دليل التثبيت
│   └── FEATURES.md                  # الميزات
│
├── 📝 README.md                     # هذا الملف
├── 📝 .gitignore                    # ملف Git Ignore
├── 📝 composer.json                 # حزم PHP
├── 📝 LICENSE                       # الترخيص
└── 📝 .env.example                  # مثال على المتغيرات
```

---

## ⚙️ متطلبات التشغيل

### 1. البرمجيات المطلوبة

| البرنامج | الإصدار | الملاحظات |
|---------|--------|---------|
| **PHP** | 7.4+ | مع دعم MySQL و GD |
| **MySQL** | 5.7+ | أو MariaDB 10.3+ |
| **Apache** | 2.4+ | مع دعم mod_rewrite |
| **Composer** | 2.0+ | مدير حزم PHP |
| **Node.js** | 14+ | اختياري (للأدوات) |

### 2. ملحقات PHP المطلوبة

```bash
php_curl.dll
php_gd2.dll
php_mysqli.dll
php_json.dll
php_mbstring.dll
php_openssl.dll
php_zip.dll
```

### 3. المتغيرات البيئية المطلوبة

```bash
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ibdaa_platform
DB_USER=root
DB_PASSWORD=
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=app-password-here
MAIL_FROM=noreply@ibdaa.com
APP_URL=http://localhost/Ibdaa-Taiz
APP_ENV=development
JWT_SECRET=your-secret-key-here
```

---

## 🚀 طريقة التثبيت

### الخطوة 1: استنساخ المستودع

```bash
git clone https://github.com/username/Ibdaa-Taiz.git
cd Ibdaa-Taiz
```

### الخطوة 2: تثبيت الحزم

```bash
# تثبيت حزم PHP
composer install

# تثبيت حزم Node.js (اختياري)
npm install
```

### الخطوة 3: إعداد قاعدة البيانات

```bash
# إنشاء قاعدة البيانات
mysql -u root -p < database/schema.sql

# إضافة البيانات الأولية (اختياري)
mysql -u root -p ibdaa_platform < database/initial_setup.sql

# إضافة بيانات نموذجية (للاختبار)
mysql -u root -p ibdaa_platform < database/sample_data.sql
```

### الخطوة 4: تكوين الملفات

```bash
# نسخ ملف المتغيرات
cp .env.example .env

# تعديل القيم حسب بيئتك
nano .env
```

### الخطوة 5: إعداد الأذونات

```bash
# تعيين الأذونات الصحيحة (Linux/Mac)
chmod 755 platform/uploads/
chmod 755 logs/
chmod 755 cache/

# تعيين الملكية
chown www-data:www-data platform/uploads/
chown www-data:www-data logs/
```

### الخطوة 6: تشغيل المشروع

```bash
# على localhost
php -S localhost:8000 -t platform/

# أو استخدام Apache مباشرة
# http://localhost/Ibdaa-Taiz/platform/
```

---

## 🔐 إعدادات الأمان

### 1. إعداد Gmail للإشعارات البريدية

#### الخطوة 1: تفعيل المصادقة الثنائية
- اذهب إلى: https://myaccount.google.com/security
- فعّل "التحقق بخطوتين" (Two-Factor Authentication)

#### الخطوة 2: إنشاء كلمة مرور التطبيق
1. اذهب إلى: https://myaccount.google.com/apppasswords
2. اختر **Mail** و **Other (Custom name)**
3. انسخ كلمة المرور المكونة من 16 حرف
4. الصقها في ملف `.env`:
```env
MAIL_PASSWORD=xxxx xxxx xxxx xxxx
```

### 2. تأمين قاعدة البيانات

```sql
-- إنشاء مستخدم بدون صلاحيات جذرية
CREATE USER 'ibdaa_user'@'localhost' IDENTIFIED BY 'secure_password_here';

-- منح الصلاحيات المحدودة
GRANT SELECT, INSERT, UPDATE, DELETE ON ibdaa_platform.* TO 'ibdaa_user'@'localhost';

-- تطبيق التغييرات
FLUSH PRIVILEGES;
```

### 3. تفعيل HTTPS (الإنتاج)

```apache
# في .htaccess
<IfModule mod_ssl.c>
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
```

### 4. حماية الملفات الحساسة

```apache
# حماية .env
<Files .env>
    Deny from all
</Files>

# حماية ملفات Config
<Files config.php>
    Deny from all
</Files>
```

---

## 📚 الميزات الرئيسية

### 1️⃣ لوحة تحكم المدير

- ✅ إدارة الطلبات والتسجيلات
- ✅ إدارة الدورات والمدربين
- ✅ إدارة المستخدمين والأدوار
- ✅ عرض التقارير والإحصائيات
- ✅ إرسال الإشعارات

### 2️⃣ لوحة تحكم المتابع الفني

- ✅ متابعة الطلبات
- ✅ تحديث حالات الطلبات
- ✅ دعم فني للمستخدمين
- ✅ تقارير المشاكل والحلول

### 3️⃣ لوحة تحكم المدرب

- ✅ إنشاء وإدارة الدروس
- ✅ إنشاء الواجبات والاختبارات
- ✅ تقييم عمل الطلاب
- ✅ متابعة الحضور
- ✅ نشر المحاضرات والموارد

### 4️⃣ لوحة تحكم الطالب

- ✅ عرض الدورات المسجلة فيها
- ✅ تحميل الواجبات
- ✅ التقديم للاختبارات
- ✅ عرض الدرجات
- ✅ تحميل الشهادات

### 5️⃣ نظام الدردشة

- ✅ دردشة فردية بين المستخدمين
- ✅ دردشات جماعية حسب الدورات
- ✅ إشعارات فورية
- ✅ حفظ السجل

### 6️⃣ نظام الجداول

- ✅ جداول زمنية للدروس
- ✅ تنبيهات الدروس القادمة
- ✅ عرض التضارب في الجداول

### 7️⃣ نظام الحضور والغياب

- ✅ تسجيل الحضور التلقائي
- ✅ تقارير الحضور
- ✅ تنبيهات الغياب المتكرر

### 8️⃣ نظام التقارير

- ✅ رسومات بيانية تفاعلية
- ✅ تقارير الأداء الأكاديمي
- ✅ تحليل بيانات الطلاب

### 9️⃣ نظام الشهادات

- ✅ إصدار شهادات تلقائية
- ✅ توقيع رقمي
- ✅ رموز QR للتحقق

---

## 🔌 API Endpoints

### المصادقة (Authentication)
```
POST   /api/auth/register      - تسجيل مستخدم جديد
POST   /api/auth/login         - تسجيل الدخول
POST   /api/auth/logout        - تسجيل الخروج
POST   /api/auth/refresh       - تحديث التوكن
GET    /api/auth/verify-email  - التحقق من البريد
```

### المستخدمين (Users)
```
GET    /api/users              - عرض جميع المستخدمين
GET    /api/users/{id}         - عرض مستخدم محدد
PUT    /api/users/{id}         - تحديث المستخدم
DELETE /api/users/{id}         - حذف المستخدم
```

### الدورات (Courses)
```
GET    /api/courses            - عرض الدورات
POST   /api/courses            - إنشاء دورة جديدة
PUT    /api/courses/{id}       - تحديث الدورة
DELETE /api/courses/{id}       - حذف الدورة
GET    /api/courses/{id}/students - الطلاب المسجلين
```

### الواجبات والاختبارات
```
GET    /api/assignments        - عرض الواجبات
POST   /api/assignments        - إنشاء واجب جديد
GET    /api/assignments/{id}/submissions - الإجابات
POST   /api/assignments/{id}/submit - تقديم إجابة
```

### الدرجات
```
GET    /api/grades             - عرض الدرجات
PUT    /api/grades/{id}        - تحديث الدرجة
GET    /api/grades/student/{id} - درجات الطالب
```

### الدردشة
```
GET    /api/chat/conversations - المحادثات
POST   /api/chat/send          - إرسال رسالة
GET    /api/chat/{id}/messages - رسائل المحادثة
```

---

## 💾 نماذج قاعدة البيانات الأساسية

### جدول المستخدمين
```sql
users:
- id (PK)
- full_name
- email (UNIQUE)
- password_hash
- role (admin, technical, trainer, student)
- phone
- governorate
- district
- photo_path
- status (active, inactive, suspended)
- created_at
- updated_at
```

### جدول الدورات
```sql
courses:
- id (PK)
- name
- description
- trainer_id (FK)
- start_date
- end_date
- duration (hours)
- capacity
- status
- created_at
- updated_at
```

### جدول التسجيل
```sql
enrollments:
- id (PK)
- student_id (FK)
- course_id (FK)
- status (pending, accepted, completed)
- enrollment_date
- completion_date
- grade
- created_at
- updated_at
```

### جدول الواجبات
```sql
assignments:
- id (PK)
- course_id (FK)
- title
- description
- due_date
- max_score
- created_at
- updated_at
```

---

## 🐛 استكشاف الأخطاء الشائعة

### ❌ مشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل:**
1. تأكد من تشغيل MySQL
2. تحقق من بيانات الاتصال في `.env`
3. تأكد من وجود قاعدة البيانات
```bash
mysql -u root -p -e "CREATE DATABASE ibdaa_platform;"
```

### ❌ مشكلة: الإشعارات البريدية لا تُرسل
**الحل:**
1. تحقق من Gmail App Password
2. تأكد من تفعيل المصادقة الثنائية
3. استخدم `test_mail.php`:
```php
php platform/test_mail.php
```

### ❌ مشكلة: ملفات الرفع لا تعمل
**الحل:**
```bash
# تأكد من وجود المجلدات
mkdir -p platform/uploads/{ids,photos,documents,certificates}

# عيّن الأذونات
chmod 755 platform/uploads/
```

### ❌ مشكلة: الجلسات لا تعمل
**الحل:**
```php
// في php.ini
session.save_path = "/tmp"
session.gc_maxlifetime = 3600
```

---

## 📊 معايير الكود

### معايير PHP
```php
// ✅ صحيح
function getUserById($id) {
    $stmt = $pdo->prepare("SELECT * FROM users WHERE id = ?");
    $stmt->execute([$id]);
    return $stmt->fetch();
}

// ❌ خطأ
function getUserById($id) {
    return mysql_query("SELECT * FROM users WHERE id = $id");
}
```

### معايير JavaScript
```javascript
// ✅ صحيح
const fetchData = async (url) => {
    try {
        const response = await fetch(url);
        return await response.json();
    } catch (error) {
        console.error('Error:', error);
    }
};

// ❌ خطأ
function fetchData(url) {
    var xhr = new XMLHttpRequest();
    xhr.open("GET", url, false); // Synchronous!
}
```

---

## 🧪 الاختبارات

### تشغيل الاختبارات

```bash
# الاختبارات الأساسية
./vendor/bin/phpunit tests/

# اختبارات محددة
./vendor/bin/phpunit tests/UserTest.php

# مع التقارير
./vendor/bin/phpunit --coverage-html coverage/
```

### ملف الاختبارات النموذجي

```php
<?php
namespace Tests;

use PHPUnit\Framework\TestCase;

class UserTest extends TestCase {
    public function testUserCreation() {
        $user = new User('Ahmed', 'ahmed@example.com');
        $this->assertEquals('Ahmed', $user->name);
    }
}
```

---

## 📈 الأداء والتحسينات

### 1. تحسين الاستعلامات
```sql
-- ✅ مع الفهارس
ALTER TABLE users ADD INDEX idx_email (email);
ALTER TABLE enrollments ADD INDEX idx_course_student (course_id, student_id);
```

### 2. التخزين المؤقت
```php
// استخدام Redis للتخزين المؤقت
$redis = new Redis();
$redis->connect('localhost', 6379);
$cached = $redis->get('courses_list');
```

### 3. ضغط الاستجابات
```php
// في Apache
AddOutputFilterByType DEFLATE text/html text/plain text/xml
```

---

## 🚢 نشر المشروع

### على Heroku

```bash
# تثبيت Heroku CLI
# ثم:
heroku login
heroku create ibdaa-app
git push heroku main
```

### على DigitalOcean

```bash
# إنشاء قطرة جديدة
doctl compute droplet create ibdaa-app \
  --region nyc3 \
  --image ubuntu-22-04-x64

# النشر
git push production main
```

### على AWS

```bash
# استخدام Elastic Beanstalk
eb init ibdaa-app
eb create production
eb deploy
```

---

## 🤝 المساهمة

نرحب بمساهماتكم! للمساهمة:

1. **Fork** المستودع
2. **أنشئ فرع** للميزة الجديدة:
```bash
git checkout -b feature/AmazingFeature
```
3. **Commit** التغييرات:
```bash
git commit -m 'Add AmazingFeature'
```
4. **Push** للفرع:
```bash
git push origin feature/AmazingFeature
```
5. **افتح Pull Request**

---

## 📞 الدعم والتواصل

- 📧 **البريد الإلكتروني**: support@ibdaa.com
- 💬 **الدردشة المباشرة**: chat.ibdaa.com
- 🐦 **تويتر**: @IbdaaTraining
- 📱 **WhatsApp**: +967-...

---

## 📄 الترخيص

هذا المشروع مرخص تحت **MIT License** - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 🙏 شكر وتقدير

شكراً خاص لـ:
- **فريق التطوير** في منصة إبداع
- **GitHub Copilot** على الدعم في التطوير
- **المجتمع اليمني** للتطوير البرمجي

---

## 📝 سجل التغييرات

### الإصدار 1.0.0 (يناير 2025)
- ✅ إطلاق النسخة الأولى من المنصة
- ✅ جميع لوحات التحكم الأساسية
- ✅ نظام الدردشة المتكامل
- ✅ نظام الواجبات والاختبارات
- ✅ API متكامل

---

**آخر تحديث**: 21 نوفمبر 2025

---

**صُنع بـ ❤️ من قبل فريق منصة إبداع**
