# 🚀 اختبار سريع للنظام - 5 دقائق

## الخطوة 1: تثبيت قاعدة البيانات (30 ثانية) ⚡

### الطريقة السهلة - Import:

**✨ لا حاجة لإنشاء قاعدة البيانات يدوياً! الملفات تعمل ذلك تلقائياً**

1. افتح **phpMyAdmin**: `http://localhost/phpmyadmin`
2. اذهب إلى تبويب **Import**
3. اضغط **Choose File** → اختر `database/exams_grades_schema.sql`
4. اضغط **Go** → انتظر ✅ "Import has been successfully finished"
5. اضغط **Choose File** مرة أخرى → اختر `database/test_data.sql`
6. اضغط **Go** → انتظر ✅ "Import has been successfully finished"

### أو الطريقة البديلة - Copy/Paste:

1. افتح `database/exams_grades_schema.sql` في Notepad
2. انسخ المحتوى كاملاً (Ctrl+A → Ctrl+C)
3. في phpMyAdmin → تبويب **SQL** → الصق (Ctrl+V)
4. اضغط **Go**
5. كرر نفس الخطوات لملف `database/test_data.sql`

**✅ تم! يجب أن ترى:**
```
✅ Test data created successfully!
Total Exams: 2
Total Questions: 17
Total Attempts: 2
Total Grade Entries: 15
```

---

## الخطوة 2: اختبار واجهة الطالب (دقيقتان)

### افتح واجهة الدرجات:
```
http://localhost/Ibdaa-Taiz/Manager/components/student_grades_widget.html
```

**ماذا يجب أن ترى؟**
- ✅ 3 بطاقات في الأعلى (إجمالي الدورات، المعدل، الشهادات)
- ✅ بطاقات الدورات مع progress bars ملونة
- ✅ تفاصيل المكونات (حضور، مشاركة، واجبات...)
- ✅ أزرار "تحميل الشهادة" للدورات المكتملة

**اختبار التحديث اللحظي:**
1. افتح `grades_entry.html` في تبويب آخر
2. غيّر أي درجة
3. ارجع لواجهة الطالب
4. انتظر 10-15 ثانية
5. **يجب أن ترى:** تحديث تلقائي + notification!

---

## الخطوة 3: اختبار واجهة المشرفين (دقيقة واحدة)

### افتح واجهة إدخال الدرجات:
```
http://localhost/Ibdaa-Taiz/Manager/grades_entry.html
```

**التجربة:**
1. اختر "دورة البرمجة المتقدمة"
2. سترى جدول بـ 3 طلاب
3. غيّر درجة أي طالب
4. لاحظ الحساب التلقائي للدرجة النهائية
5. اضغط "حفظ جميع الدرجات"

**✅ يجب أن ترى:**
- الدرجة النهائية تتحدث فوراً
- اللون يتغير (أخضر للنجاح، أحمر للرسوب)
- رسالة "تم حفظ جميع الدرجات بنجاح!"

---

## الخطوة 4: اختبار الاختبار (دقيقة واحدة)

### افتح واجهة الاختبار:
```
http://localhost/Ibdaa-Taiz/Manager/exam_interface.html?exam_id=1
```

**جرّب منع الغش:**
1. اضغط "بدء الاختبار"
2. سيُفرض وضع fullscreen
3. جرّب:
   - **Alt+Tab** (تبديل التبويبات) → ⚠️ تحذير!
   - **Ctrl+C** (نسخ) → 🚫 ممنوع!
   - **Esc** (الخروج من fullscreen) → 🔄 طلب العودة!
   - **F12** (DevTools) → ⚠️ تحذير!

4. بعد **3 تحذيرات** → إرسال تلقائي!

**✅ النتيجة:**
- جميع الأحداث مُسجلة في `exam_anti_cheat_log`

---

## تحقق من البيانات في قاعدة البيانات

### في phpMyAdmin، جرّب:

```sql
-- عرض الاختبارات
SELECT * FROM exams;

-- عرض الأسئلة
SELECT * FROM exam_questions WHERE exam_id = 1;

-- عرض الدرجات
SELECT * FROM student_grades;

-- عرض ملخص الدرجات (View)
SELECT * FROM student_grades_summary;

-- عرض سجل منع الغش
SELECT * FROM exam_anti_cheat_log;
```

---

## 🎉 إذا نجحت جميع الخطوات:

**✅ النظام يعمل بشكل مثالي!**

الملفات الجاهزة:
- ✅ Backend APIs
- ✅ Frontend Interfaces  
- ✅ Database Schema
- ✅ Test Data
- ✅ Anti-Cheat System
- ✅ Real-time Updates
- ✅ Auto Certificate Generation

---

## ❌ إذا واجهت مشكلة:

### المشكلة: لا تظهر البيانات
**الحل:**
```sql
-- تحقق من البيانات
SELECT COUNT(*) FROM users WHERE role = 'student';
SELECT COUNT(*) FROM courses;
SELECT COUNT(*) FROM exams;
```

### المشكلة: خطأ API
**الحل:**
- تحقق من `php -v` (يجب أن يكون 7.4+)
- تحقق من `extension=mysqli` في `php.ini`
- راجع `error_log` في XAMPP

### المشكلة: لا يعمل منع الغش
**الحل:**
- استخدم Chrome أو Firefox حديث
- تأكد من تفعيل JavaScript
- افتح Console (F12) وراجع الأخطاء

---

## 📞 الدعم

راجع الملفات:
- ✅ `EXAMS_GRADES_COMPLETE_GUIDE.md` - دليل شامل
- ✅ `EXAMS_GRADES_COMPLETION_REPORT.md` - تقرير الإنجاز
- ✅ `QUICK_TEST_GUIDE.md` - هذا الملف

---

**الوقت الإجمالي: 5 دقائق ⏱️**  
**الجهد: سهل جداً 😊**  
**النتيجة: نظام كامل ومتقدم 🚀**
