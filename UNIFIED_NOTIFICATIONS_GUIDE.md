# 🔔 نظام الإشعارات الموحد

## الحالة: ✅ مكتمل 100%

تم توحيد زر الإشعارات في جميع لوحات التحكم بمكون واحد موحد يعمل بنفس الطريقة في كل مكان.

---

## 📍 الملفات المستخدمة

### المكون الموحد
- **`Manager/dashboards/components/notifications-bell.php`** - المكون الرئيسي للإشعارات (HTML + JavaScript)

### لوحات التحكم
- **`Manager/dashboards/manager-dashboard.php`** - لوحة المدير ✅
- **`Manager/dashboards/technical-dashboard.php`** - لوحة المشرف الفني ✅
- **`Manager/dashboards/trainer-dashboard.php`** - لوحة المدرب ✅
- **`Manager/dashboards/student-dashboard.php`** - لوحة الطالب ✅

### مكتبات مساعدة
- **`Manager/js/dashboard-integration.js`** - API موحد للوصول إلى الإشعارات

---

## 🎯 الميزات

### ✅ الميزات المتوفرة

1. **عرض الإشعارات**
   - جلب جميع الإشعارات من قاعدة البيانات
   - عرض في نافذة منفصلة (modal)
   - ترتيب حسب الأحدث أولاً

2. **شارة العدد (Badge)**
   - عرض عدد الإشعارات غير المقروءة
   - إخفاء إذا كانت جميعها مقروءة

3. **تحديث الحالة**
   - تحديد إشعار واحد كمقروء
   - تحديد جميع الإشعارات كمقروء

4. **أنواع الإشعارات**
   - `info` - معلومات عامة (أزرق)
   - `success` - نجاح العملية (أخضر)
   - `warning` - تحذير (برتقالي)
   - `error` - خطأ (أحمر)
   - `payment` - دفعة مالية (بنفسجي)
   - `enrollment` - تسجيل في دورة (أزرق فاتح)
   - `card` - بطاقة شخصية (بنفسجي غامق)
   - `announcement` - إعلان (برتقالي)

5. **التحديث التلقائي**
   - تحديث الإشعارات كل 60 ثانية
   - لا حاجة لإعادة تحميل الصفحة

6. **واجهة استخدام سهلة**
   - زر بسيط في الـ Header
   - نافذة منبثقة جميلة
   - تصميم متجاوب

---

## 💻 كيفية الاستخدام

### الإضافة في لوحة تحكم جديدة

```php
<!-- في الـ Header -->
<div class="flex items-center gap-4">
    <!-- Unified Notifications Bell Component -->
    <?php include __DIR__ . '/components/notifications-bell.php'; ?>
    
    <!-- باقي الأزرار الأخرى -->
</div>
```

### الإضافة إلى لوحة قائمة

1. ضع المكون في الـ Header
2. تأكد من وجود `CURRENT_USER` في الـ JavaScript
3. تأكد من تحميل `dashboard-integration.js`

```php
<!-- في نهاية الملف -->
<script>
window.CURRENT_USER = {
    id: <?php echo $userId; ?>,
    name: '<?php echo addslashes($userName); ?>',
    email: '<?php echo addslashes($userEmail); ?>',
    role: 'manager'
};
</script>
<script src="../js/dashboard-integration.js"></script>
```

---

## 🔧 الوظائف المتاحة (JavaScript)

### تحميل الإشعارات
```javascript
loadNotifications()
// جلب الإشعارات من الخادم وتحديث الواجهة
```

### تحديث إشعار واحد
```javascript
markNotificationAsRead(notificationId)
// تحديث إشعار معين كمقروء
```

### تحديث جميع الإشعارات
```javascript
markAllAsRead()
// تحديث جميع الإشعارات غير المقروءة كمقروء
```

### إغلاق النافذة
```javascript
closeNotificationsModal()
// إغلاق نافذة الإشعارات
```

---

## 📡 API الخادم

### جلب الإشعارات
```javascript
DashboardIntegration.api.notifications.getAll(page, limit)
// المعاملات:
// - page: رقم الصفحة (افتراضي: 1)
// - limit: عدد الإشعارات (افتراضي: 50)
```

### إنشاء إشعار
```javascript
DashboardIntegration.api.notifications.create(
    title,           // عنوان الإشعار
    message,         // محتوى الإشعار
    type,           // نوع الإشعار
    link            // رابط اختياري
)
```

### إرسال إشعار جماعي
```javascript
DashboardIntegration.api.notifications.broadcast(
    title,          // العنوان
    message,        // المحتوى
    targetRoles     // ['student', 'trainer'] مثلاً
)
```

### تحديث حالة الإشعارات
```javascript
DashboardIntegration.api.notifications.markAsRead(notificationIds)
// notificationIds: مصفوفة من معرفات الإشعارات
```

---

## 🎨 التصميم والألوان

### أنواع الإشعارات والألوان

| النوع | اللون | الأيقونة |
|-------|-------|---------|
| success | أخضر (emerald) | check-circle |
| error | أحمر | alert-circle |
| warning | برتقالي (amber) | alert-triangle |
| info | أزرق فاتح (sky) | info |
| payment | بنفسجي | credit-card |
| enrollment | أزرق | user-plus |
| card | بنفسجي غامق (indigo) | id-card |
| announcement | برتقالي فاتح (orange) | megaphone |

---

## 📊 قاعدة البيانات

### جدول الإشعارات
```sql
CREATE TABLE notifications (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    title VARCHAR(255),
    message TEXT,
    type VARCHAR(50),           -- info, success, warning, error, etc
    link VARCHAR(500),
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

## 🔄 تحديث الإشعارات

- **التحديث التلقائي**: كل 60 ثانية
- **عند الفتح**: عند النقر على الزر
- **عند التحديث**: عند تحديث حالة الإشعار

---

## ✅ الاختبار

### تم الاختبار على:
- ✅ لوحة المدير
- ✅ لوحة المشرف الفني
- ✅ لوحة المدرب
- ✅ لوحة الطالب

### الميزات المختبرة:
- ✅ عرض الإشعارات
- ✅ شارة العدد
- ✅ تحديث الحالة
- ✅ أنواع مختلفة
- ✅ التحديث التلقائي

---

## 📝 ملاحظات مهمة

1. **dashboard-integration.js يجب أن يكون مُحمّلاً قبل استخدام المكون**
2. **CURRENT_USER يجب أن يكون معرّفاً قبل تحميل المكون**
3. **المكون يعمل بشكل مستقل ولا يحتاج إلى أي تكوين**
4. **يمكن تخصيص الألوان والرموز بسهولة**

---

## 🚀 التحسينات المستقبلية

- [ ] إضافة وسائط صوتية
- [ ] إشعارات سطح المكتب (Desktop Notifications)
- [ ] دعم WebSocket للإشعارات الفورية
- [ ] تصنيفات الإشعارات
- [ ] خيارات التصفية المتقدمة
