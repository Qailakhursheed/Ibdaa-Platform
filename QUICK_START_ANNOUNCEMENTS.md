# 🚀 دليل الاستخدام السريع - نظام الإعلانات والصلاحيات

## خطوة 1: تفعيل جدول الإعلانات ⚙️

### عبر phpMyAdmin:
1. افتح `http://localhost/phpmyadmin`
2. اختر قاعدة البيانات `ibdaa_platform`
3. اضغط على تبويب **SQL**
4. انسخ والصق هذا الكود:

```sql
CREATE TABLE IF NOT EXISTS `announcements` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `title` VARCHAR(255) NOT NULL COMMENT 'عنوان الإعلان',
  `content` TEXT NOT NULL COMMENT 'محتوى الإعلان',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT 'تاريخ النشر',
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_created_at` (`created_at` DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

INSERT INTO `announcements` (`title`, `content`) VALUES
('افتتاح دورة جديدة في البرمجة', 'نعلن عن افتتاح دورة متقدمة في تطوير تطبيقات الويب باستخدام PHP وMySQL. التسجيل مفتوح الآن!'),
('منح دراسية متاحة', 'تقدم منصة إبداع منحاً دراسية كاملة للطلاب المتفوقين. تواصل معنا للحصول على التفاصيل.'),
('ورشة عمل مجانية', 'ندعوكم لحضور ورشة عمل مجانية حول أساسيات التصميم الجرافيكي يوم السبت القادم.');
```

5. اضغط **Go** أو **تنفيذ**

---

## خطوة 2: اختبار النظام 🧪

### أ. اختبار مركز الإعلانات (للمشرف الفني):

1. **تسجيل الدخول:**
   - اذهب إلى `http://localhost/Ibdaa-Taiz/Manager/login.php`
   - سجل دخول بحساب **المشرف الفني** (technical)

2. **إضافة إعلان:**
   - انقر على **"إدارة الإعلانات"** في الشريط الجانبي (أيقونة 📣)
   - املأ النموذج:
     - **العنوان:** "دورة جديدة في الذكاء الاصطناعي"
     - **المحتوى:** "نعلن عن افتتاح دورة شاملة في الذكاء الاصطناعي..."
   - اضغط **"نشر الإعلان"**
   - يجب أن يظهر الإعلان في الجدول فوراً

3. **حذف إعلان:**
   - انقر على أيقونة 🗑️ بجانب أي إعلان
   - أكد الحذف

---

### ب. اختبار الموقع الخارجي:

1. **افتح الصفحة الرئيسية:**
   - `http://localhost/Ibdaa-Taiz/platform/index.php`

2. **تحقق من قسم الإعلانات:**
   - يجب أن ترى قسماً باسم **"آخر الإعلانات"**
   - يعرض آخر **3 إعلانات** فقط
   - كل إعلان في بطاقة جميلة بلون أزرق/بنفسجي

---

### ج. اختبار تجربة الطالب:

#### 1. الطالب غير المسجل في دورة:

**الخطوات:**
```sql
-- تأكد من عدم وجود تسجيل للطالب
DELETE FROM enrollments WHERE user_id = [رقم_الطالب];
```

- سجل دخول بحساب طالب
- **النتيجة المتوقعة:**
  - ✅ لا يوجد شريط جانبي
  - ✅ رسالة ترحيب كبيرة: "مرحباً بك في منصة إبداع!"
  - ✅ زر **"العودة إلى الصفحة الرئيسية"**
  - ✅ زر **"تصفح الدورات"**
  - ✅ قسم **"آخر الإعلانات"** (آخر 5 إعلانات)

---

#### 2. الطالب المسجل في دورة نشطة:

**الخطوات:**
```sql
-- إضافة تسجيل وهمي للطالب
INSERT INTO enrollments (user_id, course_id, status) 
VALUES ([رقم_الطالب], 1, 'active');
```

- سجل دخول بحساب الطالب
- **النتيجة المتوقعة:**
  - ✅ لا يوجد شريط جانبي
  - ✅ هيدر بسيط مع صورة الطالب
  - ✅ 3 بطاقات ملونة:
    - 🔵 **دوراتي** (أزرق)
    - 🟢 **درجاتي** (أخضر)
    - 🟣 **بطاقتي** (بنفسجي)
  - ✅ قسم "نشاطاتي الأخيرة"

---

## خطوة 3: استكشاف الأخطاء 🔧

### مشكلة: "Table 'announcements' doesn't exist"
**الحل:** عد إلى خطوة 1 وتأكد من تنفيذ السكريبت SQL

### مشكلة: الطالب يرى الشريط الجانبي
**الحل:**
```php
// تحقق من جدول users
SELECT id, email, role FROM users WHERE role = 'student';

// تأكد من أن العمود role يساوي 'student' بالضبط
```

### مشكلة: الإعلانات لا تظهر
**الحل:**
```sql
-- تحقق من وجود بيانات
SELECT * FROM announcements;

-- إضافة بيانات تجريبية
INSERT INTO announcements (title, content) 
VALUES ('إعلان تجريبي', 'هذا إعلان للاختبار');
```

---

## الميزات الرئيسية ✨

### 1. صلاحيات المشرف الفني:
- ✅ إدارة الدورات (إضافة/تعديل/حذف)
- ✅ إدارة الإعلانات (نشر/حذف)
- ✅ إدارة الشؤون المالية
- ✅ معالجة طلبات التسجيل

### 2. نظام الإعلانات:
- ✅ واجهة بسيطة للنشر
- ✅ جدول لعرض جميع الإعلانات
- ✅ حذف فوري
- ✅ ظهور تلقائي على الموقع الخارجي

### 3. تجربة الطالب المزدوجة:
- ✅ **غير مسجل:** صفحة ترحيب + إعلانات
- ✅ **مسجل:** لوحة تحكم كاملة مع بطاقات تفاعلية

---

## نصائح للاستخدام 💡

### للمشرف الفني:
1. انشر الإعلانات بانتظام لجذب الطلاب الجدد
2. اجعل العناوين قصيرة وجذابة (50 حرف كحد أقصى)
3. المحتوى يجب أن يكون واضحاً ومختصراً (150-200 حرف)

### للطلاب:
1. تحقق من قسم الإعلانات دورياً
2. اضغط على "تصفح الدورات" للتسجيل
3. بعد التسجيل، ستحصل على لوحة تحكم كاملة

---

## الدعم الفني 📞

**واجهت مشكلة؟**
- تواصل مع فريق التطوير: **RoboStack-Yemen**
- الموقع: **منصة إبداع - تعز، اليمن**

---

## ✅ قائمة التحقق النهائية

قبل الإطلاق، تأكد من:

- [ ] جدول `announcements` موجود في قاعدة البيانات
- [ ] جدول `enrollments` موجود ويحتوي على عمود `status`
- [ ] يمكن للمشرف الفني الوصول إلى "إدارة الإعلانات"
- [ ] الإعلانات تظهر على `platform/index.php`
- [ ] الطالب غير المسجل يرى صفحة الترحيب
- [ ] الطالب المسجل يرى لوحة التحكم
- [ ] زر "عرض البطاقة" يعمل (يفتح `generate_id_card.php`)

**جميع الخطوات مكتملة؟ 🎉 النظام جاهز للاستخدام!**
